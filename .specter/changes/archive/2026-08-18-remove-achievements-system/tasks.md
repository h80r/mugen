# Tasks

## 1. Audit non-easter-egg achievement rewards before deleting content
- [x] 1.1 List every reward ID in `achievements.json` NOT already covered by the default-unlock allowlist from `unlock-easter-egg-cosmetics-by-default` (i.e. regular QUANTITY/STREAK/DIVERSITY/etc. achievement cosmetics — themes, badges, display prefs earned through normal play rather than easter eggs)
  - Result: only 3 ids are uncovered — `badge_achievement_master`, `badge_week_warrior`, `display_grid_large`. Every other non-easter-egg reward id (themes, auras, titles, avatar frames, home badges, profile nickname effects, special backgrounds) is already in `ACHIEVEMENT_UNLOCKABLE_IDS`.
- [x] 1.2 Decide, with the user if ambiguous, whether these rewards should also become default-unlocked (consistent with "keep cosmetics already unlocked by default") or are acceptable to lose entirely once achievements are removed — this determines whether `isDefaultUnlockable()` needs a second, broader allowlist addition before achievements.json is deleted
  - Decision (user, confirmed after investigation): let all 3 die, do NOT add to the allowlist. Investigation showed none of the 3 ever had player-facing effect: `badge_achievement_master`/`badge_week_warrior` only appended to `UserProfile.badges`, a list read by zero UI (only touched by `BackupUserProfile`/`AchievementRestorer`) — distinct from the visual, already-covered `home_badge_*` ids. `display_grid_large` was meant to gate a library grid density but the library already uses `LibraryDisplayMode` ungated; the unlockable was never wired to it. Nothing is lost by letting them go.
  - Follow-up (user): also delete dead code tied specifically to these three/the `badge_*`/`display_*` unlockable types once the relevant later steps are reached — see notes added to tasks 6.5 and 7.1 below.
- [x] 1.3 If broadening default-unlock is needed, extend `UnlockableManager.isDefaultUnlockable()` accordingly (same mechanism as the easter-egg change) before proceeding to deletion tasks below
  - Not applicable — decision in 1.2 was not to broaden default-unlock.

## 2. Remove Achievements UI (Step 1)
**Branch:** remove-achievements-ui
- [x] 2.1 Delete `AchievementsTab.kt` and its nav entry from wherever it currently lives (More tab / Settings root)
  - `AchievementsTab.kt` itself was already unreferenced (dead since the More-section reorg). The real live nav entry was `MoreScreenAurora`'s `onAchievementsClick`/`AuroraSettingItem`, wired from `MoreTab.kt` via `AchievementScreenVoyager` — removed both, plus the now-unused `Icons.Filled.EmojiEvents` import.
- [x] 2.2 Delete `AchievementScreenVoyager.kt`, `AchievementScreen.kt`, `AchievementScreenModel.kt`
- [x] 2.3 Delete `AchievementCard.kt`, `AchievementCategoryTabs.kt`, `AchievementTabsAndGrid.kt`
  - Also deleted `AchievementContent.kt` (leaf grid renderer for the deleted screen, already had zero consumers, broken by the `AchievementScreenState` import once `AchievementScreenModel` was gone). Kept `AchievementActivityGraph`, `AchievementStatsComparison`, `ActivityStreakIndicator` (used by the Stats screen — out of scope), and `AchievementGroupNotification`/`AchievementListDialog`/`AchievementUnlockBanner`/`AchievementDetailDialog`/`AchievementIcon`/`AchievementRevealHelper`/`AchievementBannerPalette` (the unlock-popup overlay in `MainActivity.kt`, still driven by the still-live event bus — belongs to Step 2/3 teardown, not Step 1).
- [x] 2.4 Delete `AchievementScreenTest.kt` and other achievement-specific tests
  - `AchievementScreenTest.kt` (despite its name) only tests the kept `AchievementActivityGraph`/`AchievementStatsComparison` Stats components — left in place. Deleted `AchievementScreenModelTest.kt`, which directly instantiated the deleted `AchievementScreenModel`.
- [x] 2.5 Compile and fix any remaining references

## 3. Remove event emission call sites (Step 2)
**Branch:** remove-achievement-event-emitters
- [x] 3.1 Grep every remaining `AchievementEventBus` emission call site and remove each, leaving surrounding feature code untouched
  - Scope widened during execution: emission also happens via `AchievementHandler.trackFeatureUsed(...)` (not just `AchievementEventBus.tryEmit`) in browse/search/downloader/library-settings/backup/stats/settings files — all covered below.
  - [x] 3.1.1 `SetReadStatus.kt`, `SetSeenStatus.kt` (chapter/episode read-status interactors)
  - [x] 3.1.2 `BrowseAnimeSourceScreenModel.kt`, `BrowseMangaSourceScreenModel.kt`, `BrowseNovelSourceScreenModel.kt` (browse-source screen models)
    - Also removed now-write-only helpers exposed by the removal: `defaultFiltersSerialized` field (all 3), the private/internal `serializeFilters` in anime+novel (kept manga's since `BrowseMangaSourceScreen.kt` still calls it), and the `changed`/`q`/`f`/`hasActiveFilters` locals whose sole purpose was gating the tracking call.
  - [x] 3.1.3 `AnimeSearchScreenModel.kt`, `MangaSearchScreenModel.kt`, `NovelSearchScreenModel.kt` (global-search screen models)
  - [x] 3.1.4 `AnimeDownloader.kt`, `MangaDownloader.kt`, `NovelDownloadQueueManager.kt` (downloader classes)
  - [x] 3.1.5 `AnimeRepositoryImpl.kt`, `MangaRepositoryImpl.kt`, `NovelRepositoryImpl.kt` (entry repositories)
    - Constructor `eventBus: AchievementEventBus` param removed from all 3; fixed their `addSingletonFactory` call sites in `DomainModule.kt` (dropped the extra `get()`). Also removed the now-fully-dead `value.favorite?.let { ... }` blocks (their only purpose was building/emitting the LibraryAdded/LibraryRemoved event) and the now-unused `AchievementCategory` import in all 3.
  - [x] 3.1.6 `NovelScreenModel.kt`, `NovelReaderScreenModel.kt`, `NovelProgressPersistenceController.kt`, `NovelUpdatesScreenModel.kt` (novel reader/updates stack)
    - Kept every gating var/field that also drives real logic (`shouldEmitReadEvent`, `emitReadEvent`/`emitNovelCompleted` on `PendingProgressPersistence`, since they also gate `activityDataRepository.recordReading`/history-snapshot flushing/auto-track-update); only removed vars that existed solely for the achievement emit (`shouldEmitCompletion`, the `markedIds`/`willComplete` pairs, `updatesBecomingRead`). `NovelReaderScreenModel` passed its `eventBus` through to `NovelProgressPersistenceController`'s constructor — updated both sides.
  - [x] 3.1.7 `ReaderViewModel.kt`, `PlayerViewModel.kt` (manga reader / anime player)
    - `mangaId`/`animeId` locals in both files existed for the emit calls; replaced the `manga?.id ?: return` guard in `ReaderViewModel` with a plain null-check (the guard itself is real early-exit logic, not achievement-specific) and dropped the now-fully-dead `allChapters`/`allEpisodes` completion-check locals.
  - [x] 3.1.8 `AnimeLibrarySettingsScreenModel.kt`, `MangaLibrarySettingsScreenModel.kt` (library settings)
  - [x] 3.1.9 `BackupCreator.kt`, `StatsTab.kt`, `AboutScreen.kt`, `SettingsAppearanceScreen.kt`, `SettingsMainScreen.kt` (backup/stats/settings misc sites)
    - Left `AchievementBackupCreator` in `BackupCreator.kt` untouched (separate achievement-data backup/restore infra, not an emission call site — later step's territory). `AboutScreen.kt` also had a pre-existing dangling `FeatureUsageCollector` import with zero usages; removed it too since it was achievement-related dead weight in a file this task was already touching.
  - [x] 3.1.10 `DomainModule.kt` — keep the `AchievementEventBus()` DI factory itself for now (still needed while the bus exists until Step 3), only touch it if a removed constructor param changes its wiring
    - Confirmed: only the `addFactory`/`addSingletonFactory` call sites for classes whose constructor changed in 3.1.1/3.1.5 needed fixing (already done); `AchievementEventBus()`/`AchievementHandler(...)` factories themselves left untouched.
- [x] 3.2 Compile and fix any remaining references
  - Correction found during task 4: the compile check run at the time only covered `:app:compileDebugKotlin`/`:data:compileDebugKotlin` (main sources), not test sources. `:data:compileDebugUnitTestKotlin` and `:app:compileDebugUnitTestKotlin` were actually broken by 3.1.5/3.1.2/3.1.7's constructor-param removals: `MangaMemoRoundTripTest.kt`, `NovelRepositoryImplTest.kt`, `NovelCategoryRepositoryImplTest.kt` still passed a mock/real `AchievementEventBus` to `MangaRepositoryImpl`/`NovelRepositoryImpl`'s old constructor (`NovelRepositoryImplTest.kt` also had a whole test method asserting the now-removed `LibraryAdded`/`LibraryRemoved` emission — deleted it, the other two just dropped the extra constructor arg); `BrowseNovelSourceScreenModelTest.kt` had 4 whole test methods asserting `achievementHandler.trackFeatureUsed(...)` calls that no longer happen — deleted all 4 plus the `ensureAchievementHandler()` helper; `PlayerViewModelRegressionTest.kt` had a leftover `eventBus = mockk(...)` named arg. Fixed all of these as part of task 4's work since they were discovered then; all four compile tasks (`:app`/`:data` × main/test) are clean as of task 4.

## 4. Remove AchievementHandler, migrations, and the event bus (Step 3)
- [x] 4.1 Delete `AchievementHandler` (`processEvent`, tiered-progress logic, `sanitizeCrossCategoryFirstAchievements`, `recomputeGenreAchievements`)
  - **Correction to the original scope-check note**: it's wrong that `repository/AchievementRepositoryImpl.kt` and `model/AchievementJson.kt`'s sibling directory `repository/` only serve `processEvent`. Caught this only after already deleting and having to restore: `AchievementRepositoryImpl` (reads/writes `AchievementsDatabase.achievements`/`achievement_progress` directly, distinct from the rule-processing engine) is still live-consumed by `SettingsTreasuryScreen.kt`, `AchievementBackupCreator.kt`, `AchievementRestorer.kt` — all explicitly out of scope until `remove-treasury-screen`/database-removal steps. **Kept** `AchievementRepositoryImpl.kt` and its DI wiring. Similarly `StreakAchievementChecker.kt` (in `handler/checkers/`) is consumed by `HomeHubScreenModel.kt`'s live greeting-stats UI (`getCurrentStreak()` shown to users) — **kept** it and its own DI factory too (re-added `addSingletonFactory { StreakAchievementChecker(get()) }` to `DomainModule.kt`, which had been dropped when the achievement-singleton block was first collapsed). `AchievementJson.kt`, `AchievementTextResolver.kt`, `AchievementTextResolverImpl.kt` (the `app/`-side impl, found only via a second grep pass) were correctly dead (only consumer was the deleted `AchievementLoader`) and stayed deleted.
  - A background agent run for this task fabricated a completion report (files, compile results, deviations, all invented) without making any actual tool calls — none of it existed on disk. Redid the entire task directly, file by file, with grep-verified consumer checks before every deletion.
  - [x] 4.1.1 Deleted `handler/`: `AchievementHandler.kt`, `AchievementCalculator.kt`, `AchievementRuleRegistry.kt`, `RuleContextImpl.kt`, `SessionManager.kt`, `FeatureUsageCollector.kt`, `PointsManager.kt`, `AchievementEventBus.kt`, and from `checkers/`: `DiversityAchievementChecker.kt`, `FeatureBasedAchievementChecker.kt`, `TimeBasedAchievementChecker.kt` (each verified to have zero real, non-test consumers). **Kept** `checkers/StreakAchievementChecker.kt` — see correction note above.
  - [x] 4.1.2 Deleted `rules/` entirely (18 rule files + `GenreAliases.kt`) — every file verified test-only consumers before deletion.
  - [x] 4.1.3 Deleted `loader/AchievementLoader.kt`, `localization/AchievementTextResolver.kt`, `migration/AchievementMigrationHelper.kt`, `model/AchievementJson.kt`. **Kept** `repository/AchievementRepositoryImpl.kt` — see correction note above. Also deleted `app/src/main/java/eu/kanade/tachiyomi/data/achievement/localization/AchievementTextResolverImpl.kt` (the interface's sole implementation, only found via a second consumer-grep pass) and its orphaned test `AchievementTextResolverImplTest.kt`.
  - [x] 4.1.4 Removed DI wiring from `DomainModule.kt` (imports + `addSingletonFactory`/`addFactory` lines for every deleted class; re-added `AchievementRepositoryImpl`'s import after an over-broad import-block edit briefly dropped it; added back `StreakAchievementChecker`'s factory) and `AppModule.kt` (`AchievementTextResolver`/`AchievementLoader`/`PointsManager` factories — kept `AchievementRepository`/`ActivityDataRepository`/`UserProfileRepository`/`UserProfileManager` factories). In `App.kt`, removed the "Initialize achievements from JSON" launch block and the whole "Start achievement handler" block (`achievementHandler.start()`, the `unlockCallback`/`AchievementBannerManager.showAchievement` wiring, all `[ACHIEVEMENTS-INIT]` logging), the `sessionManager` field and its `onSessionStart()`/`onSessionEnd()` calls in `onStart`/`onStop` (session-tracking existed solely to feed the deleted event bus/feature collector), and the now-unused `AchievementLoader`/`AchievementBannerManager` imports. **Kept** the "clean up legacy phantom theme unlockables" and "migrate legacy activity data" launch blocks in the same `Handler.post` — both use only kept classes (`UnlockableManager`, `UserProfileManager`, `LegacyActivityDataMigrator`).
  - Also removed the now-orphaned `MainActivity.kt` popup overlay: the `AchievementGroupNotification`/`AchievementListDialog` composables and their `showAchievementsList`/`pendingAchievements` state (their only purpose was surfacing events the deleted handler no longer produces) — then deleted the now-zero-consumer `AchievementGroupNotification.kt`, `AchievementListDialog.kt`, `AchievementDetailDialog.kt` (only used by `AchievementListDialog.kt`), `AchievementRevealHelper.kt` (only used by `AchievementDetailDialog.kt`). **Kept** `AchievementUnlockBanner.kt` (defines `AchievementBannerManager`, still called from `SettingsTreasuryScreen.kt`/`ReaderActivity.kt`/`PlayerActivity.kt` — out of scope; its `MainActivity.kt` composable rendering still works, it just becomes a harmless no-op sink for those callers since nothing else registers a show-callback), `AchievementIcon.kt` (used by the kept banner), `AchievementBannerPalette.kt` (used by the kept banner; its `mutedGroupBannerGradient` half is now dead but not worth surgically splitting out of a small shared file). Deleted `RecomputeGenreAchievementsMigration.kt` and its entry in `Migrations.kt`'s migration list (task 4.2, done here since it directly imported the deleted `AchievementHandler`).
  - Deleted the now-broken test tree under `data/src/test/java/tachiyomi/data/achievement/`: `AchievementCalculatorTest.kt`, `AchievementHandlerRoutingTest.kt`, `AchievementRuleEngineTest.kt`, `EventAchievementsTest.kt`, `RuleContextTest.kt`, all of `handler/*Test.kt` and `handler/checkers/*Test.kt` except none (all handler-cluster tests deleted), `loader/*Test.kt`, `migration/AchievementMigrationTest.kt`, all of `rules/*Test.kt`, and 3 stale `.disabled` files (`ActivityDataRepositoryFixTest.kt.disabled`, `ActivityDataRepositoryTest.kt.disabled` — duplicates/precursors of the kept `ActivityDataRepositoryImplTest.kt`, confirmed not to test anything unique — `DiversityAchievementCheckerTest.kt.disabled`, `PointsManagerTest.kt.disabled`, `handler/FeatureUsageTest.kt.disabled`). **Kept** `AchievementTestBase.kt` (generic DB test scaffolding, no achievement-engine dependency), `AchievementRepositoryImplTest.kt`, `ActivityDataRepositoryImplTest.kt`, `StreakAchievementCheckerTest.kt`, `UnlockableManagerTest.kt`, `UserProfileManagerTest.kt`, `UserProfileRepositoryImplTest.kt`, `StatsReadCountSqlTest.kt` — all test classes kept in 4.1.1-4.1.3.
- [x] 4.2 Delete `RecomputeGenreAchievementsMigration.kt` — done as part of 4.1.4 above (it only compiled by importing the now-deleted `AchievementHandler`, so deleting it couldn't be deferred).
- [x] 4.3 Delete `AchievementEventBus` — covered by 4.1.1 (it lives in `handler/`, deleted there along with its `addSingletonFactory { AchievementEventBus() }` DI line in `DomainModule.kt`, left in place through Step 2 per design.md and removable now that Step 3 has no more consumers).
- [x] 4.4 Compile and fix any remaining references
  - Learned from 3.2's incomplete check: ran all four relevant compile tasks this time, not just main sources — `:app:compileDebugKotlin`, `:data:compileDebugKotlin`, `:app:compileDebugUnitTestKotlin`, `:data:compileDebugUnitTestKotlin` — all clean (exit 0) after the fixes above. Also grepped the whole repo (`.kt`/`.gradle.kts`) for every deleted class name to confirm zero remaining references outside `build/` output, and checked `app/src/androidTest` separately (not gradle-compiled in this pass) for the same set — clean.

## 5. Remove the achievements database (Step 4)
**Branch:** remove-achievements-database
- [x] 5.1 Confirm the `ActivityDatabase` copy migration has run and `ActivityDatabase.activity_log` is current
  - **Found the proposal's premise was wrong, asked the user, expanded scope with approval**: `ActivityDataRepositoryImpl` (backing `ActivityDataRepository`, used by every `recordReading`/`recordWatching`/`recordAppOpen` call site across the app — reader, player, novel stack, `SetReadStatus`/`SetSeenStatus`) still read/wrote `AchievementsDatabase.activity_log` directly. `ActivityLogRepository` (what Stats reads from, backed by `ActivityDatabase`) has zero write methods — it only ever received data via the one-time `CopyActivityLogToActivityDatabaseMigration` copy. Deleting `AchievementsDatabase` as originally planned would have silently stopped all future activity recording.
  - Fix: `data/src/main/sqldelightactivity/.../activity_log.sq` and `data/src/main/sqldelightachievements/.../activity_log.sq` turned out to be byte-for-byte identical schemas/queries (clearly designed as a drop-in replacement by `migrate-stats-and-cosmetic-selectors`, just never wired up) — repointed `ActivityDataRepositoryImpl`'s constructor from `AchievementsDatabase` to `ActivityDatabase` (same `activityLogQueries` accessor name, no query-call changes needed). Fixed the 3 tests that constructed it against the old type (`ActivityDataRepositoryImplTest.kt`, `AchievementBackupRestoreIntegrationTest.kt` — needed a second driver/database since it also uses `AchievementsDatabase` for other repos, `LegacyActivityDataMigratorTest.kt`). All new activity recording now goes straight to `ActivityDatabase`; the copy migration remains a safe one-time historical backfill from the now-frozen old DB, unaffected by this since it runs independently of app-code DI wiring.
- [x] 5.1a Resolve `UserProfile`'s DB dependency (pulled forward from task 6.1, a hard blocker for 5.2+): investigation showed `UserProfile`'s only 2 still-read fields (`unlockedThemes`, `achievementsUnlocked`) are dead weight — `unlockedThemes` is redundant with `UnlockableManager`'s default-unlock allowlist (the theme filter ORs the two, and the allowlist already covers every theme), `achievementsUnlocked` is a frozen display value nothing increments anymore since the engine is gone. User confirmed (after checking that name/avatar/nickname styling all live independently in `UserProfilePreferences`, untouched) to delete `UserProfile` entirely: the domain model, `UserProfileManager`, `UserProfileRepositoryImpl`, `UserProfileRepository` interface, `user_profile.sq`, the two live reads in `HomeHubScreenModel.loadGreetingStats()`/`AppThemePreferenceWidget`, and the theme/badge mirroring calls in `UnlockableManager.applyUnlockable`/`removeUnlockableFromProfile`.
  - [x] 5.1a.1 Renamed `AchievementBackupCreator` → `ActivityStatsBackupCreator` and `AchievementRestorer` → `ActivityStatsRestorer` (new files; old ones deleted). Stripped achievement-list/user-profile backup/restore logic including `rehydrateUnlockables` (existed purely to replay Treasury unlocks from backed-up achievement progress — moot with achievements gone); kept `backupActivityLog`/`backupStats`/`restoreActivityLog`/`restoreStats` untouched. Repointed the activity-log read from `AchievementsDatabase.activityLogQueries` to `ActivityDatabase.activityLogQueries` (same pattern as 5.1).
  - [x] 5.1a.2 Removed `backupAchievements`(600)/`backupUserProfile`(601) `ProtoNumber` fields from both `LegacyBackup` and `Backup` (two copies) and their references in `toBackup()`/`mergeLegacyPayloadIfPresent()`. Deleted `BackupAchievement.kt`/`BackupUserProfile.kt`. Updated `BackupRestorer.kt` (renamed field/call, dropped 2 params) and `BackupCreator.kt` (renamed creator field/import). **Found and fixed one file outside the original plan**: `BackupInspection.kt`'s `hasAchievements` computed field read `backup.backupAchievements.isNotEmpty()` — changed to `backup.backupActivityLog.isNotEmpty()` (kept the `hasAchievements` name itself, out of scope to rename a user-facing inspection field here).
  - [x] 5.1a.3 Deleted `UserProfile.kt` (`ProfileStats` inside it had zero consumers anywhere, deleted together), `UserProfileRepository.kt`, `UserProfileManager.kt`, `UserProfileRepositoryImpl.kt`, `user_profile.sq`. Removed DI wiring from `DomainModule.kt`/`AppModule.kt` (kept the unrelated, still-live `UserProfilePreferences` factory). Fixed `HomeHubScreenModel.kt` (dropped `userProfileManager` field and the `profile.achievementsUnlocked` read — `HomeGreetingStats.achievementCount` already defaults to 0, so omitting it is correct; the value still flows into `GreetingProvider`'s greeting-selection logic via that default, a wider refactor of `GreetingProvider` itself was out of scope) and `AppThemePreferenceWidget.kt` (dropped the redundant `unlockedThemes` check, kept `isThemePreviewUnlocked`). Fixed `UnlockableManager.kt`: dropped the `userProfileManager` constructor param, its two `applyUnlockable` mirroring calls (now just log, matching the `aura_`/`title_` branches' existing style), and left `removeUnlockableFromProfile` as an explicit no-op stub (documented why) since it's still called by `lockUnlockablesForAchievement`, which is task 7's territory, not touched here. Fixed `App.kt`'s legacy-cleanup block: removed only the `UserProfileManager`-dependent stale-theme cleanup half; **kept** `unlockableManager.removeUnlockable("theme_achievement_gold"/"sapphire")` since that's unrelated tombstone cleanup with no `UserProfile` dependency (deviates from the literal instruction to delete the whole block — deleting it would have dropped still-valid, unrelated functionality). Also removed the now-dangling `userProfileQueries` accessor from `AchievementsDatabase.kt` (found via compile error — `user_profile.sq` was its only backing table).
  - [x] 5.1a.4 Deleted `UserProfileManagerTest.kt`, `UserProfileRepositoryImplTest.kt`, `AchievementBackupRestoreIntegrationTest.kt` (every test in it was achievement/profile/tier/unlockable-rehydration specific; confirmed `ActivityDataRepositoryImplTest.kt` already independently covers `upsertActivityData`, the one behavior worth preserving). Trimmed `MihonBackupCompatibilityTest.kt`: removed its `sampleAchievement()` helper and the `backupAchievements = ...` line from its two call sites — both tests' real assertions (manga/extension-repo decoding, origin-detection marker precedence) don't depend on field 600 specifically, verified the marker-precedence test still holds via `isLegacy=false`(500), independently in `NATIVE_MARKER_FIELDS`. Deleted `AchievementTestDataGenerator.kt` (zero consumers anywhere, confirmed via grep — dead fixture code, not found via the original file list, caught by the final sweep). **Also fixed, not in the original plan**: `UnlockableManagerTest.kt` (found via the final sweep) — constructed a `stubRepo`/`stubProfileManager` and called `UnlockableManager(prefs, stubProfileManager)` 12×; removed the stub scaffolding and dropped the second constructor arg at all 12 call sites.
  - [x] 5.1a.5 All four compile targets pass: `:app:compileDebugKotlin`, `:data:compileDebugKotlin`, `:app:compileDebugUnitTestKotlin`, `:data:compileDebugUnitTestKotlin`, independently re-verified after review.
  - [x] 5.1a.6 (found during review, not in original plan) The background agent doing 5.1a.1-5.1a.5 fabricated its first completion report entirely (zero real tool calls, claimed work that didn't exist on disk) on an earlier task this change — re-verified every file this agent touched by reading the actual diffs, not just trusting its report. Found and fixed two real gaps this agent's task didn't cover:
    - `HomeGreetingStats.achievementCount`/`GreetingContext.achievementCount` fed `GreetingProvider.checkAchievementMilestone()`, which surfaces "you've unlocked 50 achievements!"-style Home Hub greetings (`achievementGreetings` list, `achievement_50`/`achievement_master`/`achievement_legendary` candidates). With the field now always 0 (nothing populates it since `UserProfile` is gone), these greetings would silently never fire again — dead code, not a compile break, so nothing caught it automatically. Removed `achievementCount` from `GreetingContext`/`HomeGreetingStats`/`HomeGreetingSelectionRequest`/`getGreeting`/`selectGreeting`/the seed computation/the `allCandidatesMap` collection, and deleted `checkAchievementMilestone()` + `achievementGreetings`. Fixed the 2 tests that referenced it (`GreetingProviderTest.kt` dropped an unrelated named-arg; `HomeGreetingSessionTest.kt` swapped its distinguishing test value from `achievementCount` to `episodesWatched`, preserving the concurrency test's actual intent).
    - The user-visible "Achievements" checkbox in the create/restore-backup screens (`BackupOptions.achievements`/`RestoreOptions.achievements`, `AYMR.strings.achievements` = "Achievements") now actually gates activity-log backup/restore, not achievements — confirmed via `ActivityStatsBackupCreator`'s `options.achievements` gate and `BackupRestorer.kt`'s `options.achievements` gate, both already repointed by 5.1a.1/5.1a.2 but still under the old flag name. Renamed the Kotlin field `achievements` → `activityLog` throughout (`BackupOptions.kt`, `RestoreOptions.kt`, `BackupRestorer.kt`, `ActivityStatsBackupCreator.kt`, `SyncPreferencesExtensions.kt`'s persisted string-key set, and 4 test files) and retexted the `achievements` string resource itself to "Activity log" (base/EN) across all 4 locale files that define it (en, pt-BR, ru, ar — confirmed via grep it's used nowhere else, the achievement screen itself was already deleted). Also renamed `BackupInspection.hasAchievements` → `hasActivityStats` (same underlying meaning shift) and its one UI consumer in `RestoreBackupScreen.kt`, and cleaned up a leftover `achievementData`/`"collect_achievements"` variable/log-label pairing in `BackupCreator.kt` to `activityStatsData`/`"collect_activity_stats"`. No backward-compat shimming needed for any of this per the user's earlier confirmation that no real backup files exist yet.
    - Re-verified: full-repo grep for every deleted/renamed symbol shows zero remaining references outside `build/`, all four compile targets re-run clean after these fixes.
  - **Stash note**: the agent doing 5.1a.1-5.1a.5 reported finding an "uncommitted change" to `data/build.gradle.kts` already on disk (the `AchievementsDatabase` sourceSet removed — task 5.2's work) and stashed it. Investigated: this was never committed or written by the user or an earlier task in this session — it was the agent's own exploratory edit from mid-task (it ran into 5.1a needing `AchievementsDatabase`-generated code that its own premature 5.2 attempt had removed, so it stashed its own change to unblock itself, then reported it as if found rather than self-caused). Dropped the stash — it's redundant with what 5.2 below will do properly once 5.1a is committed.
  - [x] 5.1a.7 Before touching `data/build.gradle.kts`, re-verified every remaining `AchievementsDatabase` consumer rather than trusting the plan's ordering. Found a second instance of the exact same stale-database bug: `StreakAchievementChecker` (backing Home Hub's displayed streak, kept per the 5.1/task-4 investigation) still read/wrote `AchievementsDatabase.activityLogQueries` directly — since all new activity writes were already repointed to `ActivityDatabase` in 5.1, this class was silently reading an increasingly stale, frozen copy. Repointed its constructor to `ActivityDatabase` (same fix pattern as 5.1) and deleted `logChapterRead`/`logEpisodeWatched`, which had zero real callers. Rewrote `StreakAchievementCheckerTest.kt` with its own `ActivityDatabase` in-memory fixture instead of `AchievementTestBase`; all tests pass at runtime (not just compile).
  - [x] 5.1a.8 Still found `AchievementsDatabase` blocked on one more real consumer: `SettingsTreasuryScreen.kt` (Treasury — explicitly out of scope for this change, `remove-treasury-screen` deletes the whole file later) reads `achievementRepository.getAll()` to build a reward→achievement lookup for "requires: [Achievement Title]" lock-veil text across 8 call sites. Confirmed this text is already unreachable in practice — every `isUnlocked` check it's gated behind already always returns `true` (all cosmetics default-unlocked since `unlock-easter-egg-cosmetics-by-default`). User approved the minimal fix: pass `emptyMap()` for `rewardToAchievementMap` at all 8 Treasury call sites, matching the pattern `SettingsCosmeticsScreen` (the Cosméticos replacement, already in scope and kept) already uses for the same shared components. Also removed the fully-dead `void_broadcast_unlocked` banner-trigger block and its `shouldShowVoidBroadcastBanner` plumbing through `MainActivity.kt`'s `INTENT_OPEN_TREASURY` handler (that easter egg was removed in `remove-void-broadcast`, so `void_broadcast_unlocked` hasn't existed in `achievements.json` for a while — missed cleanup from that change, caught here). Confirmed via grep: `AchievementRepository`/`AchievementRepositoryImpl` now have zero real feature consumers — DI wiring is the only reference. This unblocks task 6.
  - **Task ordering correction**: `AchievementsDatabase` cannot actually be deleted (5.2-5.5) before `AchievementRepositoryImpl` and the `Achievement`/`AchievementType`/`AchievementCategory` domain models it serves are gone — that dependency runs the opposite direction from how tasks.md sequenced steps 4/5. Doing task 6 (domain models + JSON) next, then returning to finish 5.2-5.5 once `AchievementRepositoryImpl` has no reason to exist.
- [x] 5.2 Remove the `AchievementsDatabase` sourceSet block from `data/build.gradle.kts`
  - Done after task 6 (see ordering correction above). Before this, re-checked `AchievementsDatabase`'s remaining consumers one more time and found one last blocker: `CopyActivityLogToActivityDatabaseMigration` (from `migrate-stats-and-cosmetic-selectors`) still reads `AchievementsDatabase.activityLogQueries` to do a one-time historical copy into `ActivityDatabase`, explicitly documented as running "ahead of the achievements system's removal." Since no real backup files or real user installs exist yet (confirmed earlier in 5.1a's planning), this migration would never have real data to copy either — user approved deleting it along with its `Migrations.kt` registry entry. Removed the sourceSet block (both `debug`/`release` `java.directories` entries, the `create("AchievementsDatabase")` database block, and the `generate*AchievementsDatabaseInterface` task dependency) from `data/build.gradle.kts`.
- [x] 5.3 Delete `data/src/main/sqldelightachievements/` entirely
  - Deleted the remaining 3 `.sq` files (`achievement_progress.sq`, `activity_log.sq`, `achievements.sq` — `user_profile.sq` was already gone from 5.1a.3) and the `migrations/` subdirectory (4 `.sqm` files).
- [x] 5.4 Delete `data/src/main/java/tachiyomi/data/achievement/database/AchievementsDatabase.kt` and its DI wiring
  - Deleted the class. Removed its DI wiring from `AppModule.kt`: the `ensureAchievementsDatabaseIsUsable`/`ensureDatabaseIsUsable` helper and its call, the `sqlDriverAchievements` `AndroidSqliteDriver` construction block, the `addSingletonFactory { AchievementsDatabase(...) }` registration, the `get<AchievementsDatabase>()` eager-init warm-up line, and the now-unused import.
- [x] 5.5 Full clean build to verify no stale sqldelight-generated code remains
  - Ran `:data:clean :app:clean` followed by `:app:compileDebugKotlin :data:compileDebugKotlin` from scratch — both pass, confirming no stale sqldelight-generated `AchievementsDatabase` code lingers in `build/`. Test compiles (`:data:compileDebugUnitTestKotlin`/`:app:compileDebugUnitTestKotlin`) also pass, after deleting `AchievementTestBase.kt` (the last remaining test scaffolding for the deleted DB — its only 2 consumers, `AchievementRepositoryImplTest.kt`/`StreakAchievementCheckerTest.kt`, had already moved to their own `ActivityDatabase` fixtures earlier in this change; it had zero remaining consumers here). Full-repo grep for `AchievementsDatabase`, `sqldelightachievements`, `tachiyomi.db.achievement`, `tachiyomi.data.achievement.database` shows zero remaining references outside `build/`.

## 6. Remove domain models and JSON content (Step 5)
- [x] 6.1 Grep every read site of `UserProfile`/`UserProfileManager` to determine whether `UserProfile` survives in slimmed form or is deleted entirely (per `design.md`'s Step 5 guidance)
  - Already resolved in task 5.1a: `UserProfile` was deleted entirely (not slimmed) — see 5.1a's notes above for the full investigation and user decision.
- [x] 6.2 Delete `Achievement`, `AchievementType`, `AchievementCategory` domain models
  - Widened the deletion beyond the 3 named types after checking every remaining consumer: also deleted `AchievementTier.kt`, `AchievementProgress.kt`, `AchievementRarity.kt`, `AchievementEvent.kt`, `Reward.kt`, `UserPoints.kt` (zero consumers), `AchievementRule.kt`/`RuleContext.kt` (the `rule/` subpackage, part of the same domain-model cluster, not caught by task 4's `data/`-only deletion). Two real blockers had to be cleared first, both found by re-checking consumers rather than trusting the task list: (1) `AchievementRepositoryImpl`/`AchievementRepository` had gone from "used by Treasury" to "zero real consumers" once Treasury's dependency was cut in 5.1a.8 — deleted both, plus their DI wiring (was duplicated in both `DomainModule.kt` and `AppModule.kt`, a pre-existing unrelated duplication, removed from both) and `AchievementRepositoryImplTest.kt`. (2) Treasury's/Cosméticos' shared `TreasuryThemeSelector`/`TreasuryAuraSelector`/`TreasuryToggleSelector` components still took `Map<String, Achievement>` params (always `emptyMap()` after 5.1a.8) — user approved refactoring the 3 function signatures and ~10 call sites across both files to drop the parameter entirely, replacing the `map[id]?.title ?: fallback` pattern with a direct fallback-string reference, since the achievement-title lookup always resolved to the fallback anyway.
  - Along the way, found and deleted a second fully-dead subsystem: the achievement unlock-popup banner (`AchievementUnlockBanner`/`AchievementIcon`/`AchievementBannerPalette`, plus `MainActivity`'s overlay call and 4 `setInReaderOrPlayer()` calls in Reader/PlayerActivity) — see task 7.1's note, done in the same pass since it was blocking `Achievement`'s deletion too.
  - Cleaned up `StreakAchievementChecker.kt`'s doc comment (double-encoded/mojibake Cyrillic, unreadable, and referenced the now-deleted `AchievementType.STREAK`) while touching the file for the `AchievementType` grep hit.
- [x] 6.3 Delete `app/src/main/assets/achievements/achievements.json`
  - Zero remaining readers (its only reader, `AchievementLoader`, was deleted in task 4).
- [x] 6.4 Delete `PointsManager` and XP/level formula code
  - Confirmed already done: `PointsManager` in task 4.1.1, `UserProfile.getXPForLevel`/`getLevelFromXP` with `UserProfile.kt`'s deletion in 5.1a.3.
- [x] 6.5 Apply the `UserProfile` decision from 6.1: delete entirely, or drop only achievement-specific fields (`titles`, `badges`, `achievements_unlocked`, `total_achievements`, XP/level fields)
  - Confirmed already done in 5.1a.3 (whole class deleted, including `badges` per task 1.2's decision).
- [x] 6.6 Compile and fix any remaining references
  - All 6 compile targets (`domain`/`data`/`app` × main/test) pass. Full-repo grep for every deleted symbol (`Achievement`, `AchievementType`, `AchievementCategory`, `AchievementTier`, `AchievementProgress`, `AchievementRarity`, `AchievementEvent`, `Reward`, `UserPoints`, `AchievementRule`, `RuleContext`, `AchievementRepository`) shows zero remaining references outside `build/` and harmless leftover comments (`// Achievement system` section dividers in `Backup.kt`/`AppModule.kt`, retexted or removed; `AuroraTheme.kt`'s "Achievement Gold color" comment left as-is, describing an unrelated color name).

## 7. Clean up UnlockableManager (Step 6, last)
**Note**: done ahead of task 6 — reordered (see task 5's ordering-correction note) because `Achievement` (the domain model) can't be deleted while `UnlockableManager` still has `Achievement`-typed params, which was the last real blocker after Treasury's fix (5.1a.8).
- [x] 7.1 Remove `unlockAchievementRewards`, `recomputeUnlockablesFromUnlockedAchievements`, `lockUnlockablesForAchievement`
  - Also removed `applyUnlockable` entirely (its per-type branches were pure logging with no remaining side effects once the `theme_`/`badge_` → `UserProfileManager` mirroring was cut in 5.1a.3 — every branch just logged, so the whole dispatch had no reason to exist once the only caller, `unlockAchievementRewards`, was gone), `removeUnlockableFromProfile` (the no-op stub from 5.1a.3, now unreachable), and `resetAllUnlockables` (doc-commented "for testing/debugging" but had zero test or production callers — its only caller was `recomputeUnlockablesFromUnlockedAchievements`). Per task 1.2's decision: also removed `isBadgeAvailable`, `isDisplayPreferenceAvailable` (zero callers), and the badge/display entries in `getUnlockableNameRes` (`badge_achievement_master`/`badge_week_warrior`/`display_grid_large` plus two previously-unnoticed dead display ids, `display_list_compact`/`display_grid_extra_large`, that were never granted by any achievement or read by any UI either). Left `getUnlockableType`'s/`isDefaultUnlockable`'s `badge_`/`display_` prefix branches in place — harmless dead classification, not worth pruning beyond what 7.1 asked for. Removed the now-fully-unused `scope`/`CoroutineScope`/`Dispatchers`/`withContext`/`launch` imports and field.
  - **Found and removed a second dead subsystem while chasing `Achievement`'s remaining consumers**: the achievement unlock-popup banner (`AchievementUnlockBanner.kt`/`AchievementBannerManager`) had zero real callers left — its only two trigger points were `App.kt`'s `AchievementHandler.unlockCallback` (deleted in task 4) and Treasury's void-broadcast trigger (deleted in 5.1a.8). Deleted `AchievementUnlockBanner.kt`, `AchievementIcon.kt` (only used by the banner), `AchievementBannerPalette.kt` (only used by the banner), the `AchievementUnlockBanner(...)` composable call in `MainActivity.kt`, and the 4 now-inert `AchievementBannerManager.setInReaderOrPlayer(...)` calls in `ReaderActivity.kt`/`PlayerActivity.kt` (set a flag nothing reads anymore). Deleted the orphaned tests `AchievementPopupCompactTokensTest.kt` and `AchievementBannerPaletteTest.kt`.
- [x] 7.2 Confirm `isUnlockableUnlocked`, `isThemeAvailable`, `isBadgeAvailable`, `isDisplayPreferenceAvailable`, `isUnlockableAvailable`, `isDefaultUnlockable`, `getUnlockableNameRes`, `getUnlockableName`, `getUnlockableType` all remain intact and unbroken
  - Corrected from the literal task text: `isBadgeAvailable`/`isDisplayPreferenceAvailable` are deliberately *not* kept, per 1.2's decision (recorded above) — they gated only the 3 dead reward ids. `isUnlockableUnlocked`, `isThemeAvailable`, `isUnlockableAvailable`, `isDefaultUnlockable`, `getUnlockableNameRes`, `getUnlockableName`, `getUnlockableType` all confirmed intact, unbroken, and still called by live UI (Cosméticos selector, Treasury, theme widget).
- [x] 7.3 Compile and fix any remaining references
  - All four compile targets (`app`/`data` × main/test) pass. Full-repo grep for every symbol removed in 7.1/the banner cleanup shows zero remaining references outside `build/`.

## 8. Retire the achievements spec
- [x] 8.1 Confirm the REMOVED spec delta in this change's `specs/achievements/spec.md` covers every requirement in `.specter/specs/achievements/spec.md`
  - Found a real gap: the archived spec has 8 requirements, the delta only covered 7 — "Diversity caching" (backed by `DiversityAchievementChecker`, confirmed deleted with zero remaining consumers) had no REMOVED entry. Added it. Also corrected two delta entries whose migration notes were written prospectively and no longer matched what actually happened: "Unlock and reward flow" now cites task 1.1/1.2's actual resolution (3 specific dead reward ids, not a hypothetical "should be added" note) instead of the pre-implementation placeholder text, and "Points, XP, and level formula" now states `UserProfile` was deleted entirely (task 5.1a) rather than the speculative "if it survives slimmed" phrasing. All 8 requirements now have matching REMOVED entries.

## 9. Verification
- [x] 9.1 Full project build and existing test suite pass
  - All 6 compile targets (`domain`/`data`/`app` × main/test) pass clean. Full `:app:testDebugUnitTest` run: 3164 tests, 2 failures — both confirmed pre-existing/unrelated to this change: `NovelReaderCacheCoordinatorTest.trims oldest caches when global budget exceeded` fails identically on `develop` (verified via a throwaway `git worktree`, neither the test nor its source have any diff from this change), `NovelReaderScreenModelTest`'s Gemini-queue coroutine timeout passes cleanly in isolation (confirmed flaky under full-suite resource contention, not a real break). A third failure (`GreetingProviderTest`) genuinely was caused by this change — removing `achievementCount` from `GreetingProvider`'s seed computation shifted the weighted-random distribution enough that a fixed-input test no longer sampled two different scenarios across its 16 draws; fixed by widening the test to 30 draws (verified by hand-replicating the seed/`weightedPick` arithmetic before and after).
- [x] 9.2 Grep the codebase for any remaining reference to `Achievement`, `achievement_` (preference/pref keys), `PointsManager` and confirm none remain outside historical spec archives
  - `PointsManager`: zero matches anywhere. `Achievement`: two matches, both correctly out of scope — `AuroraTheme.kt`'s "Achievement Gold color" comment (an unrelated color name, verified earlier) and `AchievementStatsComparison`/`AchievementActivityGraph` in two `androidTest` files (`AchievementScreenTest.kt`, `AchievementActivityGraphUITest.kt`) — both explicitly kept Stats-screen components, no achievement-engine dependency. `"achievement_` pref keys: one match, `"achievement_unlockables"` — the live SharedPreferences filename backing `UnlockableManager`'s kept cosmetic-unlock state; renaming it would wipe every existing user's unlocked cosmetics on upgrade, correctly left alone.
- [x] 9.3 Confirm the Stats screen's streak/comparison/yearly-activity sections still work correctly (unaffected by this change, per `migrate-stats-and-cosmetic-selectors`)
  - Verified on a physical device (debug build, `dev.h80r.mugen.localdev`): Stats screen renders the current streak ("1 dia"), a month-over-month comparison card with real data, and the yearly activity graph, all correctly populated. No crashes or exceptions in logcat.
- [x] 9.4 Confirm the Cosméticos selector screen still shows every cosmetic (easter-egg and, per task 1, any broadened default-unlock set) as selectable
  - Verified on-device: Settings → Aparência → Cosméticos opens the selector. "Efeitos visuais e auras" lists every aura as a normal selectable card, including former achievement-reward auras (e.g. "Aura de Mestre das Conquistas") and easter-egg auras (e.g. "Aura de Fusão do Núcleo", currently selected) — none locked or hidden.
- [x] 9.5 Confirm Treasury (still present) still renders without crashing, now showing everything unlocked
  - Verified on-device: Mais → Tesouraria renders fully. Vault hero shows "52/52 recompensas desbloqueadas · 100% de poder de coleção" with the debug "Ignorar bloqueios para teste" bypass confirmed OFF (i.e. the 52/52 reflects real default-unlock logic, not the debug bypass) — briefly toggled on by an accidental tap during testing, confirmed via screenshot, then toggled back off to restore original state. Temas (Mítico/Secreto) and Efeitos visuais e auras sections render correctly below. No crashes in logcat.
  - **Found, investigated, and scoped out a pre-existing cosmetic issue**: the Vault hero's featured "AURA" pill displays "VOID BROADCAST R..." — a stale value read from the on-device `enabledAuras` preference (`SettingsTreasuryScreen.kt`'s `TreasuryVaultHero` call takes `uiPreferences.enabledAuras().get().firstOrNull()` verbatim, prettified, with no cross-check against the current `allAuraPalettes()` list). "Void Broadcast" was removed as an easter egg in an earlier, unrelated change (`remove-void-broadcast`, commit `9bfd808f6`) — confirmed via grep that zero live code references it (`VOID_BROADCAST`/`Void Broadcast`/`VoidBroadcast` only appear in two explanatory comments). This is stale local device preference state from before that removal, not a defect introduced by `remove-achievements-system` and not a crash — out of scope for this change.
