# Tasks

## 1. Data layer
- [x] 1.1 Add `novel_quotes` table + queries to a new `.sq` file (`data/src/main/sqldelightnovel/datanovel/novel_quotes.sq`), following `novel_history.sq`'s pattern: `insert`, `getAll` (joined through `novel_chapters`/`novels` for title/chapter display, ordered by `saved_at DESC`), `delete`
- [x] 1.2 Run/regenerate SQLDelight codegen and confirm the new table builds

## 2. Domain layer
- [x] 2.1 Add `tachiyomi.domain.quote.novel.model.NovelQuote` domain model
- [x] 2.2 Add `NovelQuoteRepository` interface (`tachiyomi.domain.quote.novel.repository`) — also added `NovelQuoteWithRelations` model to carry joined display data (novel title, chapter name/number) for `getAll`, mirroring `NovelHistory`/`NovelHistoryWithRelations`'s split
- [x] 2.3 Add `GetNovelQuotes`, `InsertNovelQuote`, `DeleteNovelQuote` interactors
- [x] 2.4 Implement `NovelQuoteRepositoryImpl` in `data/src/main/java/tachiyomi/data/quote/novel/`, backed by the new `.sq` queries
- [x] 2.5 Register the repository/interactors in the relevant DI module (mirroring how `history/novel`'s equivalents are registered) — also wired the `novel_quotesAdapter` (Date column adapter) into the `NovelDatabase` construction in `AppModule.kt`, required for the new table's generated queries to compile

## 3. Save-quote action in the reader
**Branch:** feature/add-quote-save-action

Replaces the "Share" action with "Save quote" in the Aurora selection action console (`NovelSelectedTextActionConsole`), per user decision — see `design.md`'s "Save action" section. The native `ActionMode` menu (dictionary/translation) is untouched.

- [x] 3.1 Replace `SHARE` with `SAVE_QUOTE` in `NovelSelectedTextConsoleAction` (`NovelSelectedTextTranslationModels.kt:25-31`)
- [x] 3.2 In `NovelSelectedTextActionConsole.kt`, swap the `SHARE` branch's label/icon for `SAVE_QUOTE` (new string resource `novel_reader_text_selection_action_save_quote` in `i18n-aniyomi` base + `pt-rBR`, bookmark-style icon in place of `Icons.Default.Share`)
- [x] 3.3 In `NovelReaderContentHost.kt`'s console `onAction` handler (~line 4177), replace the `SHARE` branch (share intent) with a `SAVE_QUOTE` branch that resolves the current `chapterId` from reader state and calls `InsertNovelQuote` with the selection text, then clears the selection — added `NovelReaderScreenModel.saveSelectedTextAsQuote(text)` (scroll-aware chapter resolution mirroring `toggleChapterBookmark`) and wired it through `NovelReaderScreenActions.onSaveQuote`
- [x] 3.4 Add a small confirmation (snackbar/toast) when a quote is saved — `Toast` with new string `novel_reader_text_selection_quote_saved`, matching this file's existing `Toast.makeText` confirmation pattern
- [x] 3.5 Note: end-to-end manual verification of this task depends on text selection actually working, which is fixed by `improve-novel-reader-behavior-settings` — if that change hasn't landed yet, verify locally via a temporary workaround or wait for it before final manual testing

## 4. Quotes list screen and More-tab entry
**Branch:** feature/add-quotes-list-screen
- [x] 4.1 Add a `QuotesScreen` (or equivalent Voyager `Screen`) that queries `GetNovelQuotes` and renders a chronological list: quote text, source novel title, "Novel" type label, chapter label, saved timestamp — added `NovelQuotesScreen` (ui/quote/novel), `NovelQuotesScreenModel`, and presentation components under `presentation/quote/novel/`, following `NovelHistoryScreen`'s plain (non-Aurora-glass) list pattern
- [x] 4.2 Add delete affordance per row (swipe or long-press), backed by `DeleteNovelQuote` — per-row delete `IconButton` + confirmation `AlertDialog` (`NovelQuoteDeleteDialog`), matching `NovelHistoryItem`'s existing tap-to-delete pattern rather than swipe
- [x] 4.3 Add "Citações" as a new `AuroraSettingItem` entry in `MoreScreenAurora.kt`, following the existing `onCategoriesClick`/`onStatsClick`/`onHelpClick` wiring pattern, and wire its navigation in `MoreTab.kt`
- [x] 4.4 Manually verify: save a quote while reading a novel, open Mais > Citações, confirm it appears with correct source/timestamp, then delete it and confirm it's gone — verified by the user across this session (the 4.5 crash was found during this pass, fixed, and quotes have since been saved/viewed successfully multiple times)
- [x] 4.5 Fix crash found during 4.4 manual testing: `SQLiteException: no such table: novel_quotes` on an existing install — `novel_quotes.sq`'s bare `CREATE TABLE` only applies to fresh databases; added `data/src/main/sqldelightnovel/migrations/26.sqm` (the missing incremental migration) so upgrading installs actually get the table

## 5. Tap a quote to open the reader at that position
User-requested extension after 4.4 manual testing (not in the original proposal). Full plan: `/home/h80r/.claude/plans/cheerful-giggling-starfish.md`. Approach: search-by-text-content on open (no schema change, no char offset persisted) — reuses `buildWebReaderTtsSyncJavascript` for WebView, block-index scrolling for native scroll/page reader, and book mode's unused `BookSeekReason.Search` for exact seeking via `BookLocator`. Every renderer must fall back to normal open behavior if the quote text can't be found.
- [x] 5.1 Wire `NovelQuoteItem.onClick` through `presentation/quote/novel/NovelQuotesScreen.kt` (`onClickQuote` param) to `ui/quote/novel/NovelQuotesScreen.kt`, navigating to `NovelReaderScreen(chapterId = quote.chapterId, seekQuoteText = quote.text)`
- [x] 5.2 Add `seekQuoteText: String?` to `NovelReaderScreen`'s and `NovelReaderScreenModel`'s constructors; expose a one-shot `consumePendingSeekQuoteText(forChapterId: Long): String?` on the screen model, scoped to the opening chapter only; expose the same via a `host` hook to `NovelBookReaderController`
- [x] 5.3 Add `findQuoteTextBlockMatch` (new file `NovelReaderQuoteSeek.kt`) — whitespace-normalized substring search over chapter text blocks, reusing `NovelPageReaderTtsPaging.kt`'s normalization helpers; wire it into native scroll's initial index resolution in `NovelReaderContentHost.kt` (plain renderer only; rich native scroll explicitly out of scope for v1, falls back to normal resume) — added `onConsumePendingSeekQuoteText` to `NovelReaderScreenActions` since the composable has no direct screen-model reference
- [x] 5.4 Add `resolvePageIndexForBlock` next to `resolvePlainPageReaderTtsAnchors` in `NovelPageReaderTtsPaging.kt`; wire block-match → page lookup into page reader's initial page resolution in `NovelReaderContentHost.kt` (covers both plain and rich page reader) — generalized `resolvePageIndexForBlock` to take `List<List<Int>>` block indices rather than a specific slice type, and `findQuoteTextBlockMatch` to take `List<Pair<Int, String>>` (index, text) so each renderer supplies its own index space (list position for native scroll, `sourceBlockIndex` for page reader) instead of assuming they coincide
- [x] 5.5 Wire WebView seek: at every `onPageFinished`/`restoreWebViewScroll(` site in `NovelReaderContentHost.kt` (two, confirmed exhaustive by grep), call `buildWebReaderTtsSyncJavascript(snippet = pendingSeekQuoteText, ...)` instead of the normal scroll restore when a pending seek exists
- [x] 5.6 Add `NovelBookArtifactSource.locatorOfQuoteInChapter(chapterId, quoteText): BookLocator?` — reads the chapter's artifact HTML slice via `NovelBookArtifact.readRange`, searches normalized block text via Jsoup, converts the matched `data-o` (whole-book) offset via `artifact.locatorOf(charOffset)`; wrapped in `runCatching`. Wired into `NovelBookReaderController.maybeStartBookMode` to override `resumeLocation` and use `BookSeekReason.Search` instead of `BookSeekReason.Resume` when a match is found — also fixed `onBookSeekApplied`'s restoring-position cover to clear on `Search` too, not just `Resume` (a real bug I'd have introduced otherwise: a non-start quote seek would set `bookModeRestoringPosition` but never clear it)
- [x] 5.7 Add unit tests: `findQuoteTextBlockMatch` (exact/normalized/no-match/blank), `resolvePageIndexForBlock`, `locatorOfQuoteInChapter` (also confirms `data-o` is whole-book, not chapter-relative) — the `locatorOfQuoteInChapter` tests caught a real bug: `Jsoup.parseBodyFragment(html).body()` wraps the parsed `<section>` inside `<body>` as a single child, so `body().children()` was iterating over `[<section>]` instead of the section's own block children; fixed by selecting `section.nb-chapter` explicitly before iterating
- [x] 5.8 Manually verify on-device: tap a quote in book mode opens at the exact sentence; tap a quote in classic mode (native scroll / page reader / WebView, whichever sources are available) opens at the right spot; chapter next/prev after a quote-seek open behaves normally (no stale re-seek) — confirmed working by the user
