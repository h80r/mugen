# Tasks

## 1. Remove Aurora Heart
**Branch:** remove-aurora-heart
- [x] 1.1 Delete `eu/kanade/domain/easteregg/aurora/` (10 files)
- [x] 1.2 Delete `eu/kanade/presentation/easteregg/aurora/` (13 files)
- [x] 1.3 Remove the "Reset Aurora Heart" debug entry
- [x] 1.4 Remove the About-screen hidden logo tap sequence that triggers Aurora Heart
- [x] 1.5 Compile and fix any remaining references

## 2. Remove Lattice Resonance
**Branch:** remove-lattice-resonance
- [x] 2.1 Delete `eu/kanade/domain/easteregg/lattice/` (7 files)
- [x] 2.2 Delete `eu/kanade/presentation/easteregg/lattice/` (10 files on disk vs. 8 in the proposal; kept `LatticeProtocolLiveColors.kt` and trimmed `LatticeMotion.kt` to just `rememberLatticeReducedMotion()` — both are theme-only code with no quest dependency, feeding `TachiyomiTheme.kt`'s still-functioning `latticeProtocolOverlay()`, mirroring the AURORA_PRIME resolution in group 1)
- [x] 2.3 Remove the "Lattice Grid" and "Force Lattice Breach" debug entries (also removed the manual "Open the Lattice" More-tab entry and the Reset Lattice Resonance debug entry, since all three lost their backing manager)
- [x] 2.4 Compile and fix any remaining references (included the hidden `LatticeCarrierSlot` hold-to-latch triggers embedded in reader/player chrome — not called out in the proposal's file inventory)

## 3. Remove Void Broadcast
**Branch:** remove-void-broadcast
- [x] 3.1 Delete `MeltdownRitual.kt`, `RealityBreach.kt`, `GlitchStack.kt`, and sibling components (9 files on disk vs. 8 in the proposal: `MeltdownRitual.kt`, `RealityBreach.kt`, `GlitchStack.kt`, `GlitchStackShader.kt`, `MeltdownInitiation.kt`, `TerminalGlitchDialog.kt`, `CouncilCodeLock.kt`, `RiftCorruptShader.kt`, `RiftDatamosh.kt` — `CouncilCodeLock.kt` and `RiftCorruptShader.kt`/`RiftDatamosh.kt` were undocumented "Step 3" quest components only reachable via `MeltdownInitiation.kt` and `SettingsAdvancedScreen.kt`'s `GlitchRiftWidget`; keep `WeepingVoidShader.kt`, which is shared AURORA theme background code with no quest dependency despite living in the same directory, mirroring the AURORA_PRIME/Lattice theme-code resolutions in groups 1-2) under `presentation/components/`
- [x] 3.2 Remove the "Glitch Rift" debug entry (removed the `meltdownStage == 1` conditional block and the now-dead `GlitchRiftWidget` composable, both in `SettingsAdvancedScreen.kt`, plus their exclusive imports and the `settings_advanced_glitch_rift_title` string in en/pt-BR; left `UiPreferences.meltdownStage()` itself for 3.3 and the `meltdown_terminal_*`/`meltdown_reward_*` strings for 4.2, since `ReaderActivity.kt` still references them until its own Void Broadcast hook is removed)
- [x] 3.3 Remove `UiPreferences.meltdownStage()` and the `pref_meltdown_stage` preference key (removing it forced fixing every caller in the same pass: the "third impact"/"третий удар" search-trigger listeners embedded in all three GlobalSearchScreen.kt files, the swipe-into-void triggers in PagerViewer.kt/WebtoonViewer.kt, and ReaderActivity.kt's full Step-2/Step-3 ritual — the meltdownSwipeCount/triggerMeltdownFinalRitual block that inserted the void_broadcast_unlocked achievement directly, which is the same hook task 4.2 targets; also removed the now-fully-dead meltdown_* strings (68 keys × 4 locales) since nothing calls them anymore)
- [x] 3.4 Compile and fix any remaining references (folded into 3.3 above; `:app:compileDebugKotlin` is clean and a codebase-wide grep for every Void Broadcast symbol/preference returns nothing)

## 4. Clean up shared integration points
- [x] 4.1 In `App.kt`, remove the bootstrap hooks that directly insert achievements/unlockable state for easter-egg completion (verified: `App.kt` has zero `AchievementRepository`/`insertOrUpdateProgress` references — Aurora's and Lattice's hooks were removed in groups 1-2, and Void Broadcast never had a hook here; its direct-insert lived in `ReaderActivity.kt` and was removed as part of task 3.3)
- [x] 4.2 In `ReaderActivity.kt`, remove the direct Void Broadcast achievement insertion (done as part of task 3.3's `triggerMeltdownFinalRitual()` removal, which contained the `void_broadcast_unlocked` `insertOrUpdateProgress` call; verified zero `AchievementRepository` references remain in the file)
- [x] 4.3 In `AchievementCard.kt`, remove the `isAuroraHeart`/`isLatticeResonance` branches from the shared conditional block, leaving the rest of the card intact (verified: file has zero references to `isAuroraHeart`/`isLatticeResonance`/`isVoidBroadcast`/SECRET-category special-casing — already fully cleaned by groups 1-2, and group 3 confirms nothing Void-Broadcast-related was hiding here either)

## 5. Clean up achievements.json content
- [x] 5.1 Remove SECRET-category entries whose sole purpose is representing Aurora Heart, Lattice Resonance, or Void Broadcast completion (per the ID list produced in `unlock-easter-egg-cosmetics-by-default` task 1.1-1.4: `aurora_heart`, `lattice_resonance`, `void_broadcast_unlocked` — 114→111 entries; their reward IDs stay covered by `UnlockableManager`'s default-unlock allowlist from that same change, unaffected by this removal. Left `AchievementTextResolverImpl.kt`'s now-unreachable `when` branches for these IDs alone — that's generic achievement-system text-resolution infra in `remove-achievements-system`'s scope, not easter-egg-specific logic, same as every other achievement's branch there. Left `SettingsTreasuryScreen.kt`/`MainActivity.kt`'s Void Broadcast banner-trigger code alone too — Treasury is explicitly out of scope per this proposal, owned by `remove-treasury-screen`; it's now unreachable since the deep-link that fired it was removed in task 3.3, but Treasury's own change will clean up its side)
- [x] 5.2 Confirm no non-easter-egg achievement entry references a removed entry (e.g. via META-type dependencies) — fix or drop any that do (checked all 111 remaining entries for any mention of the 3 removed achievement IDs or their 9 reward IDs: zero matches; the 14 `type: "meta"` entries — `master_achiever`/`achievement_hunter`/`achievement_collector`/`achievement_completionist` counting unlocked-achievement totals, `rank_up_1`-`10` counting points — reference no achievement by ID and their thresholds, 10-250000, are unaffected by going from 114→111 total achievements)

## 6. Retire the easter-eggs spec
- [x] 6.1 Confirm the REMOVED spec delta in this change's `specs/easter-eggs/spec.md` covers every requirement in `.specter/specs/easter-eggs/spec.md` (found a gap: the baseline's "Debug-only overrides" requirement — covering "Force Lattice breach"/"Reset Lattice Resonance" — had no matching REMOVED entry; added one. Confirmed the baseline's "Achievement reward cosmetics are unconditionally unlocked" requirement correctly has no REMOVED entry, since it's owned by `unlock-easter-egg-cosmetics-by-default` and survives this change; also confirmed no Void-Broadcast-specific mechanism requirement exists anywhere in the baseline spec to remove — it was never documented as its own requirement, only referenced as an example inside the surviving cosmetics-unlock requirement. All 8 requirements that should be removed now have exactly matching REMOVED entries)

## 7. Verification
- [x] 7.1 Full project build and existing test suite pass (`:app:assembleDebug` clean; `:data:testDebugUnitTest` fully green; `:app:testDebugUnitTest` has 2 failures — `NovelReaderCacheCoordinatorTest` and a `NovelReaderScreenModelTest` Gemini-queue timing test — both pre-existing/unrelated: neither test file nor the source under test appears anywhere in `git diff develop...HEAD`, `NovelReaderCacheCoordinatorTest` fails identically on a clean `develop` checkout, and the other is a real-wall-clock 1000ms timeout in an unrelated Gemini-translation-cache test, consistent with load-sensitive flakiness rather than a regression)
- [x] 7.2 Grep the codebase for any remaining reference to `easteregg`, `Aurora` (quest-related, not the `AURORA` theme/AppTheme itself), `Lattice`, `Meltdown`, `GlitchStack`, `RealityBreach` and confirm none remain outside the theme system (zero matches for `AuroraHeart`/`AuroraQuest`/`AuroraVault`/`AuroraNight`/`AuroraEchoBus`/`AuroraRiddle*`, `LatticeCarrier`/`LatticeVault`/`LatticeSignalBus`/`LatticeBoardModel`/`LatticeOverlayHost`/`LatticeGridScreen`/`LatticeRewardsScreen`/`LatticeProtocolManager`, and `Meltdown`/`GlitchStack`/`RealityBreach` entirely. The `easteregg.lattice` package name and its two files persist deliberately — documented in 2.2 as theme-only code (`LatticeProtocolLiveColors.kt`, trimmed `LatticeMotion.kt`) feeding `TachiyomiTheme.kt`. Remaining `aurora_heart`/`lattice_resonance`/`void_broadcast_unlocked` string-key and `AchievementTextResolverImpl.kt` `when`-branch hits are generic achievement-text-resolution infrastructure, out of scope here per the proposal — owned by `remove-achievements-system`, now simply unreachable dead branches since their JSON entries are gone)
- [x] 7.3 Manually verify every easter-egg reward cosmetic is still selectable via the Cosméticos screen and still shows unlocked in Treasury (installed a debug build on a physical device via `claude-in-android`, bumped versionCode 229→230/versionName 0.63.15. Treasury shows "52/52 recompensas desbloqueadas · 100% de poder de coleção" featuring LATTICE PROTOCOL theme and VOID BROADCAST aura. Cosméticos screen: "Aura de Fusão do Núcleo" (Void Broadcast's aura_void_broadcast_red) selected with no lock; "Vazio Chorando" (Void Broadcast's special_background_void_weeping_red) selectable; "Navegação Celestial" (Aurora Heart's special_navbar_aurora_celestial) and "Borda de Circuito" (Lattice Resonance's special_navbar_lattice_circuit) both selected with no lock)
- [x] 7.4 Manually walk the About screen, debug menu, and reader/library screens to confirm no dead trigger surfaces (tap sequences, long-press hotspots, search-trigger listeners) remain (tapped the About-screen mugen logo 8x rapidly — no Aurora Heart dialog/overlay, no crash, confirming task 1.4's removal; visited Settings → Sistema → Avançado → Depuração — no "Glitch Rift" entry, matches task 3.2's removal; visited the More screen fully scrolled — no "Open the Lattice" entry, matches task 2.3. Could not drive an actual global-search "third impact" trigger or reader swipe-into-void, since this test device has no library/sources configured to reach those screens with content — that gap is covered instead by the code-level verification in 3.3/3.4/7.2: compile-clean, and a full-codebase grep confirms zero remaining references to the search-trigger listeners, PagerViewer/WebtoonViewer swipe hooks, or meltdownStage. `logs.sh` showed no errors throughout the session)
