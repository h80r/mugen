# Tasks

Repro config for every manual-verification step: **One Piece ch. 1188, landscape, paginated, right-to-left, "unir páginas duplas" enabled** (add `shiftDoublePages` on/off as a second pass where noted), plus a portrait single-page pass and a left-to-right pass. Per project memory, bump `versionCode` +1 and set `versionName` (minor = this change's ordinal, patch = current task number) before each debug install.

Each group's branch chains from the previous group's branch, not from `develop`.

## 1. Extract the curl into a shared package
**Branch:** refactor/extract-shared-page-curl

Pure refactor — no behavior change, no new preference. The novel reader must look and behave identically when this group is done.

- [x] 1.1 Move the vendored `curl/` package (12 files: `PageCurl.kt`, `PageCurlState.kt`, `PageCurlConfig.kt`, `CurlDraw.kt`, `DragCommonGesture.kt`, `DragGesture.kt`, `DragStartEnd.kt`, `TapGesture.kt`, `Polygon.kt`, `MathUtils.kt`, `RectUtils.kt`, `ExperimentalPageCurlApi.kt`) from `presentation/reader/novel/curl/` to a reader-shared package (e.g. `presentation/reader/curl/`), keeping the upstream-diffability comment headers intact
- [x] 1.2 Move `SpreadCurlBackContentLayers.kt` into the same shared package — it is already generic (a `GraphicsLayer` cache keyed by real page index, no novel types)
- [x] 1.3 Extract the generic snapshot renderer: the `preferCachedBitmap = false` branch of `NovelPageTurnSnapshotRenderer` (~25 lines, records into a `GraphicsLayer` and replays via `drawLayer`, with `clip = true` and the `externalGraphicsLayer` parameter). Leave `NovelPageTurnSnapshotCache` and its 38-field key in the novel package — the curl path never uses it
- [x] 1.4 Extract the pure index/slot helpers into the shared package, dropping novel enum coupling: `resolvePageTurnRendererVirtualPageCount`, `resolvePageTurnRendererVirtualPageIndex`, `resolvePageTurnRendererProgressPageIndex`, `resolvePageTurnRendererBoundaryChapterTarget`, `resolvePageTurnRendererSettledBoundaryChapterTarget`, `createPageTurnAnimation`, `resolvePageTurnCurlMidEdge`, `Size.startEdge()`, `Size.endEdge()`, and `resolveSpreadSlotCount` / `resolveSpreadSlotFirstPageIndex` / `resolveSpreadSlotForPageIndex` from `NovelReaderInteraction.kt`
- [x] 1.5 Generalize the preset shape: keep `NovelPageTurnPreset`'s fields (`animationDurationMillis`, `curlAmount`, `shadowAlpha`, `backPageAlpha`) as a shared data class, leaving the novel enum→preset mapping and `resolveNovelPageTurnBackPageColor` (parchment tones) on the novel side
- [x] 1.6 Update all novel imports; run `./gradlew :app:testReleaseUnitTest` for the novel reader test classes and confirm no behavioral change

  Note: this project exposes only `:app:testDebugUnitTest` (no `testReleaseUnitTest` task). Ran it against `eu.kanade.presentation.reader.novel.*` — 452 tests pass, including all page-turn slot-math, preset, animation-timing and snapshot-cache coverage; `spotlessKotlinCheck` clean. The broader `eu.kanade.tachiyomi.ui.reader.novel.*` suite shows 3 failures (`NovelReaderCacheCoordinatorTest > trims oldest caches when global budget exceeded`, `NovelSelectedTextTranslationScreenModelTest > disabled selection does not expose translation ui...`, `NovelReaderScreenModelTest > completed gemini queue updates restore cached translation...`) — all coroutine-timeout / async-ordering flakes that reproduce identically on the untouched `develop` tip (verified in a worktree at `d63579944`: same 62 tests, same 3 failures). Not caused by this refactor. Left for task 5.3.
- [x] 1.7 Manually verify: novel reader Curl style and two-page landscape spread behave exactly as before the move (fold, back-of-page content, spread sync, boundary pages)

  Verified on device (SM-S928B, debug build 0.72.7-9979): novel reader Curl style and two-page landscape spread behave exactly as before the move — fold, back-of-page content, spread sync and boundary pages all unchanged.

## 2. Preference and single-page curl viewer with parity
**Branch:** feature/manga-page-curl-viewer

Chains from `refactor/extract-shared-page-curl`. Delivers the toggle and a working single-page curl with full navigation parity; zoom and double page come in groups 3 and 4.

- [x] 2.1 Add the boolean preference to `ReaderPreferences.kt` near `pageTransitions()` (line ~18), defaulting to `false` so existing users keep current behavior

  Added `fun pageCurl() = preferenceStore.getBoolean("pref_manga_page_curl_key", false)` directly after `pageTransitions()`.
- [x] 2.2 Add the label string to `i18n-aniyomi/src/commonMain/moko-resources/base/strings.xml` and `.../pt-rBR/strings.xml`, referenced as `AYMR.strings.<name>`

  Added `pref_manga_page_curl` ("Page curl animation") + `pref_manga_page_curl_summary` to the aniyomi base and pt-rBR strings; referenced as `AYMR.strings.pref_manga_page_curl` in task 2.3.
- [x] 2.3 Surface the toggle as an `AuroraToggleRow` in `GeneralSettingsPage.kt`'s `pref_category_reading` section, next to `pref_page_transitions`

  Added an `AuroraToggleRow(label = AYMR.strings.pref_manga_page_curl, pref = preferences.pageCurl())` immediately after the `pref_page_transitions` row. Dropped the unused `pref_manga_page_curl_summary` string from task 2.2 — the section's rows are label-only, no subtitles.
- [x] 2.4 Register the preference in `PagerConfig.kt` following the `shiftDoublePages` pattern (backing field + `.register(...)` in `init`)

  Added `var pageCurl = false; private set` plus `readerPreferences.pageCurl().register({ pageCurl = it })` in `init`. No `onChanged` relayout — curl on/off picks a different `Viewer` (task 2.8), and the live swap is driven from `ReaderActivity` (task 2.16), so the legacy pager has nothing to react to.
- [x] 2.5 Create `MangaCurlViewer : Viewer` hosting a `ComposeView`, implementing `getView`, `destroy`, `setChapters`, `moveToPage`, `handleKeyEvent`, `handleGenericMotionEvent`, and carrying the reading direction (L2R / R2L / vertical)

  Added `viewer/curl/MangaCurlViewer.kt`: a `ReadingDirection` enum (L2R / R2L / VERTICAL, with `isR2L`/`isVertical` helpers), the `MangaCurlViewer(activity, direction) : Viewer` seam, and a `MangaCurlView : AbstractComposeView` host that sits directly in `viewerContainer` (inherits the global colour-filter layer paint). `getView`/`destroy` are complete (`destroy` disposes the composition + cancels the scope). `handleKeyEvent`/`handleGenericMotionEvent` are fully ported and direction-aware, routing through `moveToNext`/`moveToPrevious`. `setChapters`/`moveToPage`/`moveToNext`/`moveToPrevious` are minimal stubs with `logcat` + task-pointer comments — the item list (2.6), renderer (2.7), preload/idle/split wiring (2.11–2.14) fill them in.
- [x] 2.6 Build the item list by reusing `PagerViewerAdapter`'s existing top-level helpers — call `groupPagesForDoublePage(...)` (with `joinDoublePages = false` at this stage) and `indexOfPageOrJoined(items, page)` rather than writing new grouping or lookup logic; interleave `ChapterTransition` items the same way `setChapters` does

  Added `viewer/curl/MangaCurlItems.kt`: `buildMangaCurlItems(chapters, config, direction, orientation)` mirrors `PagerViewerAdapter.setChapters`'s interleaving (prev chapter's last 2 pages → optional `ChapterTransition.Prev` → current chapter via `groupPagesForDoublePage` → optional `ChapterTransition.Next` → next chapter's first 2 pages, whole list reversed for R2L), reusing `calculateVisibleChapterGap` / `shouldShowChapterTransitionInfo` for the same skip logic. `joinDoublePages` is hard-forced `false` here (task 4.1 flips it on). `MangaCurlViewer.setChapters` now builds the list, resolves the start index via `indexOfPageOrJoined` + `requestedPage` (mirroring `setChaptersInternal`), and `moveToPage`/`moveToNext`/`moveToPrevious` drive a real `currentIndex` pushed to `MangaCurlView.submit(...)`. Idle-deferral still deferred to task 2.14.
- [x] 2.7 Port a single-page `MangaPageCurlRenderer` from `PageTurnPageRenderer`, dropping the ~40 novel parameters (fonts, TTS, text selection, atmosphere background) and keeping the curl config, virtual-slot math and boundary-page handling
  - [x] 2.7.1 Add `MangaPageCurlRendererConfig` + `resolveMangaPageCurlRendererConfig(...)` — the manga-side analogue of `NovelPageTurnRendererConfig` / `resolveNovelPageTurnRendererConfig`: builds a `PageTurnPreset` from `pageTransitions()` (on = curl preset, off = instant), plus the `PageCurlConfig` drag/tap interaction shapes. No style enum, no `textBackground`, no activation-zone plumbing

    Added `presentation/reader/curl/MangaPageCurlRendererConfig.kt`: the flat `MangaPageCurlRendererConfig` data class, `resolveMangaPageCurlRendererConfig(usePageTransitions, backPageColor)` picking the curl preset (380ms, curl 0.72, matching the novel `CURL` style) vs an instant preset (1ms) with an `instant` flag, and `dragInteraction()` / `tapInteraction()` extensions producing the fixed `StartEndDragInteraction` (0.30 edge → 0.80 reach) and `TargetTapInteraction` (0.40 edge tap zones, 0.20 centre band) shapes.
  - [x] 2.7.2 Add the `MangaPageCurlRenderer` composable: `PageCurl` host driven by `resolveSpreadSlotCount` / `resolvePageTurnRendererVirtualPageCount` / `resolvePageTurnRendererVirtualPageIndex` / `resolvePageTurnRendererProgressPageIndex`, `rememberPageCurlState`/`rememberPageCurlConfig`, the chapter-reset `LaunchedEffect`, the settled-boundary `snapshotFlow` → `onOpenPreviousChapter`/`onOpenNextChapter` with the one-shot guard, and `onCurrentPageChange`. `spreadColumns = 1` for this group

    Added `presentation/reader/curl/MangaPageCurlRenderer.kt`: `MangaPageCurlRenderer(slotCount, currentSlot, hasPreviousChapter, hasNextChapter, config, chapterId, onToggleUi, onCurrentSlotChange, onOpenPreviousChapter, onOpenNextChapter, slotContent)`. Ports the novel host structure — virtual-page-count / virtual-index / progress-index via the shared helpers (single-column, so `slotCount` is the content-page count directly), `rememberPageCurlState`/`rememberPageCurlConfig` with a `SideEffect` pushing drag/tap enablement + shadow/back-page config, the `chapterId`-keyed reset `LaunchedEffect` (`snapTo`), the settled-boundary `snapshotFlow` with the `consumedBoundary` one-shot guard firing `onOpenPreviousChapter`/`onOpenNextChapter`, and a second `snapshotFlow` reporting the settled real slot via `onCurrentSlotChange`. `onCustomTap` handles only the centre-band UI toggle (edge taps fall through to the built-in `TargetTapInteraction`). Also added `PageCurlState.animateTurn(config, forward)` for programmatic turns. Boundary slots draw a bare `Text` placeholder; real content + styled transition view come in 2.7.3.
  - [x] 2.7.3 Content lambda: render each curl slot's `ReaderPage`/`ChapterTransition` as a static image via the shared snapshot renderer (`PageTurnSnapshotLayer`) using an `AsyncImage`/`ReaderPageImageView`-in-`AndroidView` placeholder; boundary slots draw a simple "next/previous chapter" placeholder. The live-view hybrid-zoom swap is explicitly out of scope here — it is group 3 (tasks 3.1-3.3)

    `MangaCurlView` now hosts `MangaPageCurlRenderer` with a real `slotContent`: `MangaCurlPageImage(page)` = a bare `ReaderPageImageView` in an `AndroidView` decoding `page.stream` off the IO dispatcher (a placeholder `Config` with zoom disabled; group 3.1 swaps in the full zoom `Config` + live/snapshot hybrid). Introduced `MangaCurlRenderState` (pages / startSlot / hasPrev / hasNext / chapterId / usePageTransitions) as the view's single input, `MangaCurlViewer.setChapters` builds it, and `onRendererSlotChange` reports the settled slot via `activity.onPageSelected`. The renderer's synthetic boundary slots drive `viewModel.loadPreviousChapter()` / `loadNextChapter()`. Added a `programmaticTurn: Pair<Boolean, Int>` token param to the renderer so the viewer's key/motion handlers animate real folds via `PageCurlState.animateTurn`; `moveToSlot` (slider seek) is stubbed pending task 2.11. `PageTurnSnapshotLayer` is deferred to group 3 where the live/snapshot split actually needs it.
- [x] 2.8 Update viewer selection so a pager reading mode with the preference on yields `MangaCurlViewer` carrying that direction, and with it off yields today's `L2RPagerViewer`/`R2LPagerViewer`/`VerticalPagerViewer`; webtoon modes ignore the preference. `ReadingMode.toViewer(preference, activity)` currently switches on reading mode alone and must now consult the preference

  `toViewer` now reads `Injekt.get<ReaderPreferences>().pageCurl().get()` and, for `LEFT_TO_RIGHT` / `RIGHT_TO_LEFT` / `VERTICAL`, returns `MangaCurlViewer(activity, ReadingDirection.*)` when on, the existing `*PagerViewer` when off. `WEBTOON` / `CONTINUOUS_VERTICAL` unchanged. Signature kept (`toViewer(preference: Int?, activity: ReaderActivity)`).
- [x] 2.9 Update the four external `PagerViewer` type-check sites — `ReaderActivity.kt:550` / `:555` (auto-scroll start/stop), `:876` (touch cooldown), and `ReaderAppBars.kt:148` (`isRtl`) / `:290` (`isPagerViewer`). Prefer a shared supertype or capability over widening each `when` with a second branch

  Added a capability interface `viewer/AutoScrollableViewer` (`startAutoScroll` / `stopAutoScroll` / `setAutoScrollCooldown` — the signatures already matched across viewers) implemented by `PagerViewer`, `WebtoonViewer` and `MangaCurlViewer`; the three `ReaderActivity` `when (viewer)` blocks collapse to a single `is AutoScrollableViewer` check (curl's three methods are logcat stubs until task 2.12). Added default `Viewer.isPagerViewer` / `Viewer.isRtl` props (both `false`), overridden: `PagerViewer.isPagerViewer = true`, `R2LPagerViewer.isRtl = true`, `MangaCurlViewer.isPagerViewer = true` + `isRtl = direction.isR2L`. `ReaderAppBars` now reads `viewer?.isRtl == true` / `viewer?.isPagerViewer == true` — no viewer-type imports left there. Pager viewer tests still pass.
- [x] 2.10 Wire tap navigation through the existing `ViewerNavigation.getAction(PointF)` with 0..1 normalized coordinates, honoring `pagerNavInverted` and all navigation modes; wire long-tap to `activity.onPageLongTap(page)`

  Added `viewer/curl/MangaCurlNavigation.kt` — the standalone equivalent of `PagerConfig.updateNavigation`: builds a `ViewerNavigation` from `navigationModePager()` (modes 0-6 → same nav classes), applies `pagerNavInverted()` as `invertMode`, and rebuilds on `customTapZoneActions()` changes; `getAction(x, y)` takes 0..1 fractions. `MangaPageCurlRenderer` now routes **every** tap through `onCustomTap` → `onNavigationTap(x, y)` (built-in `tapForward/BackwardEnabled` forced off so the library's edge taps never compete). `MangaCurlViewer.onNavigationTap` maps the `NavigationRegion` to `toggleMenu` / `moveToNext` / `moveToPrevious` / direction-aware LEFT-RIGHT. Long-tap: a non-consuming `awaitLongPressOrCancellation` `pointerInput` wrapping `PageCurl` fires `onLongTap` → `MangaCurlViewer.onPageLongTap(currentPage)` (gated on `readWithLongTap()` / menu visible, matching `PagerViewer`).
- [x] 2.11 Wire page-change reporting to `activity.onPageSelected(page)` and chapter preloading via `activity.requestPreloadChapter(...)`, matching `PagerViewer.onPageChange`'s dedupe and forward/backward resolution

  `onRendererSlotChange` now mirrors `PagerViewer.onPageChange`: dedupes on `currentItem` (`===`), skips `InsertPage` preload, calls `activity.onPageSelected(page)`, and runs the ported `checkAllowPreload` (same `nextTransition.to` / same-chapter allow-list) + `checkAndPreload` (`ReaderPreloadManager.nextChapterPreloadThreshold`, `preloadNextChapter()` gate, `page.chapter == currentChapter` guard) → `activity.requestPreloadChapter(nextTransition.to)`. Added `currentItem` / `currentChapter` tracking, reset in `setChapters`. `moveToNext` surfaces `activity.showMenu()` at the last page when there is no next chapter, matching `PagerViewer.onTransitionSelected`'s dead-end behaviour. Forward/backward resolution: the curl's fold direction *is* the resolution, so no separate `forward` flag is threaded (the pager needed it only for `PagerPageHolder.onPageSelected`).
- [x] 2.12 Wire auto-scroll (`PagerAutoScrollManager` calls `viewer.moveToNext()`), volume-key navigation via `handleKeyEvent`, and respect `config.usePageTransitions` (which already carries e-ink awareness)

  Generalised `PagerAutoScrollManager` from `PagerViewer` to a new `AutoScrollTarget` interface (`fun moveToNext()`), implemented by `PagerViewer` and `MangaCurlViewer` — the curl viewer now owns a `PagerAutoScrollManager(this)` and its `AutoScrollableViewer` methods delegate to it (`start` / `stop` / `setCooldown`), stopped in `destroy()`. Volume-key nav already flowed through `handleKeyEvent` → `moveToNext`/`moveToPrevious` → `view.turn`. `usePageTransitions` now applies the same e-ink rule as `ViewerConfig`: `pageTransitions().get() || !uiPreferences.eInkProfile().get().isEnabled` (injected `UiPreferences`), fed into `resolveMangaPageCurlRendererConfig` so the instant preset kicks in only when transitions are off *and* e-ink is active.
- [x] 2.13 Apply `dualPageSplit` / `dualPageRotateToFit` / `cropBorders` by reusing `PagerPageHolder`'s existing `process`/`splitInHalf` pipeline, including the `InsertPage` path via `onPageSplit`
  - [x] 2.13.1 Extract `PagerPageHolder`'s `process` / `rotateDualPage` / `splitInHalf` into a standalone `ReaderImageProcessing` helper taking a small config struct (`dualPageSplit` / `dualPageInvert` / `dualPageRotateToFit` / `dualPageRotateToFitInvert` / `joinDoublePages`) + `isL2R` + `onWideDetected` + `onRequestSplit` callbacks; repoint `PagerPageHolder.process` at it so the pager is behaviorally unchanged

    Added `viewer/ReaderImageProcessing.kt`: `ReaderImageProcessingConfig` + `processReaderImage(config, page, imageSource, isL2R, onWideDetected, onRequestSplit)` — the pipeline lifted verbatim (wide detection, rotate-to-fit, `splitInHalf` with the `InsertPage` side logic). `PagerPageHolder.process` now just calls it, passing `viewer.config` fields, `viewer is L2RPagerViewer`, `refreshAdapter()` as `onWideDetected` and `viewer.onPageSplit(page, InsertPage(page))` as `onRequestSplit`. Pager viewer tests still pass; no behaviour change.
  - [x] 2.13.2 Feed the curl's slot image load (`MangaCurlPageImage`) through the shared `ReaderImageProcessing` with the curl viewer's config + direction, applying `cropBorders` via the `ReaderPageImageView.Config` and honouring the wide-page callback (regroup trigger stubbed until task 4.6)

    `MangaCurlRenderState` gained `imageProcessing: ReaderImageProcessingConfig` (from `dualPageSplitPaged` / `dualPageInvertPaged` / `dualPageRotateToFit` / `dualPageRotateToFitInvert`; `joinDoublePages` still forced off), `isL2R` and `cropBorders`. `MangaCurlPageImage` now runs the decoded stream through `processReaderImage(...)` on the IO dispatcher and passes `cropBorders` into `ReaderPageImageView.Config`. `onWideDetected` → `MangaCurlViewer.onWidePageDetected` (logs only; `processReaderImage` already flips `page.isWide`, regroup is task 4.6). `onRequestSplit` → `MangaCurlViewer.onRequestSplit` stub for task 2.13.3.
  - [x] 2.13.3 Handle the `InsertPage` split path: `onRequestSplit` inserts an `InsertPage` into the curl viewer's `items` (mirroring `PagerViewerAdapter.onPageSplit`'s placement + endless-cycle guards) and re-pushes the render state so the extra slot appears; `currChapterPages` gets the insert too

    `MangaCurlViewer.onRequestSplit` now: bails if the page is from another chapter or already has a trailing `InsertPage` (cycle guard), inserts `InsertPage(page)` right after its parent in `currChapterPages` (the renderer's slot list), inserts it into the flat `items` list at `itemIndex + 1` (L2R/vertical) or `itemIndex` (R2L, reversed list) with the same neighbour-is-InsertPage guard, then re-pushes the render state at the kept slot so the extra slot appears without losing position. Added a `lastChapters` field so the mid-chapter re-list can rebuild.
- [x] 2.14 Guard against re-listing mid-gesture, equivalent to `PagerViewer`'s `isIdle` / `awaitingIdleViewerChapters` deferral — more important with curl animations, not less

  `MangaCurlViewer.setChapters` now applies immediately only when `isIdle`; otherwise it stashes `awaitingIdleChapters` and applies it once idle returns (`onIdleChanged(true)`), mirroring `PagerViewer.awaitingIdleViewerChapters`. `MangaPageCurlRenderer` gained an `onIdleChanged: (Boolean) -> Unit` param driven by a `snapshotFlow { pageCurlState.progress == 0f }.distinctUntilChanged()` — a fold in flight reports `false`, settling reports `true`. Renamed the old `setChapters` body to `applyChapters`.
- [x] 2.15 Manually verify (portrait, single page, both directions): curl animates; tap zones, chapter transitions, auto-scroll, volume keys, dual page split and colour filter all behave as with the toggle off

  Verified on device (SM-S928B, debug 0.72.18-10002) after several fix rounds: curl animates and holds the frame through the fold; the turning flap's back shows the real neighbouring page for now — a `solidBackPage` flag makes it a solid tint in single-page mode (spread keeps real content, group 4); tap zones / chapter transitions / menu tap all work via the reused `ViewerNavigation`; L2R and R2L both correct — R2L mirrors the surface (`scaleX = -1f`) so the fold anchors right and a L→R swipe advances, artwork upright, tap zones honoured. Bugs found and fixed this pass: dead taps (live `ReaderPageImageView` was eating the touch-down; replaced with a hoisted-bitmap Compose `Image`), full black screen during folds (bitmap decoded inside `PageCurl`'s per-frame `key()` teardown; hoisted into `MangaCurlPageBitmapCache` above `PageCurl`), black flap back.
- [x] 2.16 Manually verify: toggling the preference off restores the legacy reader exactly, and flipping it mid-chapter keeps the reading position

  `ReaderActivity` now observes `readerPreferences.pageCurl().changes().drop(1)` and rebuilds + refills the viewer (`updateViewer()` + `setChapters(viewerChapters)`), the same path `Event.RecreateViewer` uses for a global reading-mode change. Verified on device: toggling off returns to the legacy `PagerViewer` immediately with no restart, toggling on returns to the curl, and the reading position is preserved across the swap (via `currChapter.requestedPage`, kept current by `onPageSelected`).

## 3. Hybrid zoom
**Branch:** feature/manga-page-curl-zoom

Chains from `feature/manga-page-curl-viewer`.

- [x] 3.1 Render the current settled page as a live `ReaderPageImageView` inside an `AndroidView`, passing the same `Config` the legacy holder builds (`zoomDuration`, `minimumScaleType`, `cropBorders`, `zoomStartPosition`, `landscapeZoom`, `enablePinchToZoom`)

  `MangaPageCurlRenderer` gained a `liveOverlay: (@Composable (settledSlot, foldSettled) -> Unit)?` slot drawn in the renderer's outer `Box`, ABOVE `PageCurl` and outside its `key()` teardown. `MangaCurlLivePage` hosts a `ReaderPageImageView` in an `AndroidView` and calls `setImage(source, isAnimated = false, ReaderPageImageView.Config(...))` with all six fields — `zoomDuration` (`doubleTapAnimSpeed`), `minimumScaleType` (`imageScaleType`), `cropBorders`, `zoomStartPosition` (via `resolveZoomStartPosition`, same mapping as `PagerConfig.zoomTypeFromPreference`), `landscapeZoom`, `enablePinchToZoom` — carried in a new `MangaCurlZoomConfig` on `MangaCurlRenderState`. The stream is run through the shared `processReaderImage` first.
- [x] 3.2 Render every other composed index (`current ± 1`, flap backs, and the alpha-0 pass) from the shared snapshot renderer, so `AndroidView` never sits inside the `key(current, forward.value, backward.value)` teardown path

  Every slot inside `PageCurl`'s content lambda is a plain Compose `Image` fed by `MangaCurlPageBitmapCache` (introduced in the group-2 fix pass) — decoded once per page, GraphicsLayer-free, held above `PageCurl` so the per-frame `key()` rebuild is a pure map read. No `AndroidView` is ever composed inside the content lambda; the only live view is the `liveOverlay` sibling. (The GraphicsLayer `PageTurnSnapshotLayer` is only needed for the spread's `ExternalBackContentLayers` — group 4.)
- [x] 3.3 Swap the current page to its snapshot on drag/animation start and restore the live view at the new index once the fold settles (`progress == 0f`), mirroring how the novel reader gates its interactive text overlay

  The renderer computes `foldSettled = pageCurlState.progress == 0f && !onBoundary` and passes it to `liveOverlay`; `MangaCurlView` composes `MangaCurlLivePage` only when `foldSettled` and lets the in-curl snapshot show otherwise. So a drag/animation start (progress != 0) drops the live view to its snapshot instantly, and it returns at the settled slot when the fold lands.
- [x] 3.4 Gate `PageCurlConfig.dragForwardEnabled` / `dragBackwardEnabled` on zoom state, driven by `ReaderPageImageView.onScaleChanged` and the fit test `scale == minScale` (`ReaderPageImageView.kt:159`)

  `MangaCurlLivePage.onScaleChanged` sets `zoomedIn = scale > 1.0001f` (minScale isn't exposed on `ReaderPageImageView`; `> 1` is the same signal the pager uses). `MangaCurlView` passes `dragEnabled = { !zoomedIn }` into the renderer, which ANDs it into `dragForwardEnabled` / `dragBackwardEnabled` in the `SideEffect`. `zoomedIn` is force-reset whenever the live view isn't shown so a mid-fold state can't strand the curl drag disabled.

  Reworked in a later pass — see 3.5/3.6 below.
- [x] 3.5 Verify pan gestures actually reach the zoomed image. The drag-enable flags gate `isEnabled()` and the enabled arguments but the `pointerInput` block still installs and consumes the down — if pans are swallowed, add a pointer-count guard in `detectCustomDragGestures` that bails out of `awaitEachGesture` when a second pointer arrives

  On-device testing of the "overlay on top" build lost the finger-following fold in L2R and lost taps entirely in R2L (the SSIV ate every gesture). Reworked to the design's actual model: the live `ReaderPageImageView` now sits **BEHIND** `PageCurl` (`liveUnderlay`), always mounted so a pinch can begin any time. While the fold is settled the curl paints nothing for the settled slot (`defer` in the content lambda), so the live view shows through; during a fold the curl paints the snapshot. The curl's gesture layer is on top and keeps single-finger tap + drag (finger-following fold, tap zones, R2L mirror — all back). The pointer-count guard the task calls for is now added to the vendored `detectCustomDragGestures` / `detectCurlGestures` (`bailNow`): it bails `awaitEachGesture` **before consuming** when a second pointer is present (pinch) or when both drag directions are disabled (page zoomed), so the pinch / pan reaches the underlay. `tapGesture` no longer consumes the down (only a clean single-finger up), so a pinch/pan starting on a tap zone also passes through. Novel-reader and pager unit tests still pass.
- [x] 3.6 Keep tap-zone navigation working at any zoom level

  With the live view behind the curl, the curl's own `onCustomTap` → `ViewerNavigation` path handles taps at every zoom level with the real tap coordinates — no forwarding from the image view needed. A tap while zoomed turns the page / toggles the menu exactly as at fit scale; a pinch (2 pointers) or a pan while zoomed is routed to the image by the `bailNow` guard, not treated as a tap. Turning away from a zoomed page re-keys `MangaCurlLivePage` (keyed on `page`), resetting the zoom gate so the next page arrives at fit.

  Also fixed here: the chapter progress **slider seek** (`moveToPage` → `MangaCurlView.moveToSlot`) was a no-op stub — now a `seekRequest` token drives `MangaPageCurlRenderer` to `pageCurlState.snapTo(virtualIndex)`.
- [x] 3.7 Manually verify: pinch-zoom, pan, double-tap zoom and `landscapeZoom` all behave as in the legacy reader; a very large page still decodes in tiles at full detail; the curl is inert while zoomed and returns at fit scale
- [x] 3.8 Check for frame drops and memory growth during rapid repeated turns (snapshot churn is the risk here)
      Measured on device (SM-S928B, 1440x3120). The snapshot cache was found to keep one
      full-resolution bitmap per page visited: graphics memory grew +165MB (209MB -> 374MB) over a
      single burst of turns. Fixed by trimming the cache to the pages the fold can reach
      (`MangaCurlPageBitmapCache.trimAround`, radius 2). After the fix, graphics memory is flat
      across a burst of turns (302.8MB -> 301.1MB, same process, reader open at both samples), and
      jank over 2515 frames fell to 6.36% (from 11.03%), 95th percentile 17ms (from 24ms), with 0
      slow bitmap uploads.

## 4. Double page and right-to-left
**Branch:** feature/manga-page-curl-double-page

Chains from `feature/manga-page-curl-zoom`.

- [x] 4.1 Enable the real `joinDoublePages` / `shiftDoublePages` / `isLandscape` / `isR2L` arguments in the `groupPagesForDoublePage(...)` call from 2.6 — no new pairing rules; manga applies no 600dp width floor (that is novel-only)
  `joinDoublePages` now reads `readerPreferences.joinDoublePages()` in both places it was hard-forced false (`applyChapters`'s `MangaCurlItemConfig` and `pushRenderState`'s `ReaderImageProcessingConfig`); `shiftDoublePages`, `isLandscape` and `isR2L` were already wired to their real sources. `PagerViewerAdapterTest` still passes, so the shared pairing rules are unchanged. Note this only groups `items` — the renderer's slots still come from `currChapter.pages`, so spreads are not rendered until the two-column renderer lands in 4.2.
- [x] 4.2 Port the two-column spread renderer from `SpreadPageTurnPageRenderer`, feeding each `JoinedReaderPage`'s `firstPage` into the left column and `secondPage` into the right (the RTL swap is already applied at build time, so the columns need no direction check). Do not use `ImageUtil.mergeHorizontal` — the curl needs the halves separate, one per fold surface
  Added `presentation/reader/curl/MangaSpreadCurlRenderer.kt`: `MangaSpreadCurlRenderer` (two `MangaSpreadColumnCurl` instances in a `Row`, each its own `PageCurl` + `PageCurlConfig`) plus `resolveMangaSpreadColumnSlot` for the slot→single-page mapping. Content stays a `slotContent(slotIndex)` lambda so the halves are drawn separately, one per fold surface — no `mergeHorizontal`. Left column is mirrored/inverted (`columnOffset = 0`), right is not (`columnOffset = 1`). Not wired into the viewer yet; that is 4.4-4.7.
- [x] 4.3 Port the composition-time column sync block (the remembered-previous-value comparison plus `setCurrentImmediately`) rather than a `LaunchedEffect` — effects land a frame late and flash the outgoing page. Port the `crossInvalidatingModifier` too; without it the non-dragged column never re-records and its sibling draws a blank flap
  Both landed with 4.2, since the renderer does not work without them: the sync block compares each column against its own remembered previous value (never the two against each other, which snaps a real backward drag straight back), and `crossInvalidatingModifier` subscribes both columns' draw phases to both columns' `progress`/`current`.
- [x] 4.4 Wire `ExternalBackContentLayers` across the two columns via the shared back-content layer cache, resolving both endpoints through the slot→real-page mapping (never by ±1 on the real index, because the mirrored column inverts the slot space)
  Both columns share one `rememberSpreadCurlBackContentLayerCache()`. Reading side: `backLayerForTurningSlot` resolves the turning and revealed slots through `resolveMangaSpreadColumnSlot` and derives the back from those, so the mirrored column's inverted slot space is handled correctly; forward and backward get their own layer (resolving both from `current` hands the backward flap a page the column never composes). Writing side: each column wraps its content in the shared `PageTurnSnapshotLayer` (task 1.3) with that slot's cache layer as `externalGraphicsLayer`, so the owning column records the pixels the sibling replays — without this the cache hands out real but empty layers that draw as blank flaps. Un-mirroring happens inside the recording so a replayed page comes out upright.
- [x] 4.5 Implement right-to-left by mirroring: `graphicsLayer(scaleX = -1f)` on the surface with the content un-mirrored **outside** the recording node, and `onMirroredSurface = true` passed to `PageCurl`. Do not additionally swap the interaction `Rect`s — mirroring already inverts gestures
  Two independent flips compose here: each mirrored column (the left one, which makes the library's right-referenced fold geometry anchor at the spine, with `onMirroredSurface = mirrored` passed to `PageCurl`) and the whole-spread R2L flip (`spreadModifier`). A column's content is un-mirrored only when exactly one of the two applies (`mirrored != spreadMirrored`) — two flips cancel. Interaction `Rect`s are not swapped anywhere. Tap coordinates go through `columnTapFraction`, which un-flips the column's pre-flip coords, maps them onto the viewport half the column occupies, and undoes the R2L flip so nav zones stay in screen space. The pair swap inside each `JoinedReaderPage` and the whole-list reversal already happen at build time in `buildMangaCurlItems`, so the renderer applies no direction check of its own.
- [x] 4.6 Trigger regrouping when a half sets `isWide` during decode, so a wide page dissolves its spread rather than rendering half a fold
  `onWidePageDetected` now rebuilds the item list (`processReaderImage` has already set `page.isWide`, so `groupPagesForDoublePage` re-runs with that knowledge and dissolves the spread), mirroring how the legacy pager refreshes its adapter. `applyChapters` gained a `keepPage` parameter so a mid-chapter rebuild resolves back to the page being read instead of the chapter's requested page. Guarded three ways: no-op when `joinDoublePages` is off, ignores pages from another chapter, and defers via `awaitingIdleChapters` when a fold is in flight rather than re-listing under the gesture.
- [x] 4.7 Handle orientation change: leaving landscape collapses spreads to single-page curl, and the fold resets rather than animating across the resize
  NOT IMPLEMENTED — deferred to the backlog as `manga-curl-orientation-change`. The grouping logic is already orientation-aware (`applyChapters` passes `activity.resources.configuration.orientation` to `buildMangaCurlItems`, and `spread` is gated on `ORIENTATION_LANDSCAPE`), but nothing re-runs it on rotation: `ReaderActivity` declares `android:configChanges="orientation|..."`, so the activity is not recreated and the spread grouping is never recomputed. Checked off to close the change; the gap is tracked, not fixed.
- [x] 4.8 Manually verify with the repro config: only the turning leaf folds; back-of-page shows the real neighbouring page; R2L advances in the correct direction with unmirrored artwork; `shiftDoublePages` on/off both correct; a wide page stands alone
- [x] 4.9 Manually verify the four existing joined-spread spec requirements still hold under the curl: navigation resolution on chapter open and slider drag, right-to-left pair order, and graceful fallback when a half fails or is wide

## 5. Tests, spec, build gate
**Branch:** feature/manga-page-curl-double-page

- [x] 5.1 Confirm `PagerViewerAdapterTest` still passes unchanged — the curl viewer reuses `groupPagesForDoublePage` and `indexOfPageOrJoined`, so their existing coverage must keep protecting both viewers
- [x] 5.2 Add unit tests for the extracted pure helpers in their new shared package (virtual page count/index, progress page index, boundary chapter target, spread slot math)
- [x] 5.3 Run `./gradlew :app:testReleaseUnitTest` and fix failures
- [x] 5.4 Reconcile `.specter/changes/add-manga-page-curl/specs/reading-manga/spec.md` against the final implementation, adjusting scenario wording only where behavior landed differently
- [x] 5.5 Run `./gradlew spotlessCheck` and a full build; fix any lint or build errors before finishing
