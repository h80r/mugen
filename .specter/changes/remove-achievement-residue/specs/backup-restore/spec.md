# Delta for Backup & Restore

## REMOVED Requirements

### Requirement: Manual backup triggers achievement tracking
(References `AchievementEvent.Feature.BACKUP`, which no longer exists anywhere in code following the achievement engine's removal in `remove-achievements-system`. This requirement is stale documentation drift, not a live behavior.)
